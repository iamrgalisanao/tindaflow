# TindaFlow POS — Project Manifest

## Purpose

This is the single tracking document for TindaFlow's progress against the
governing project brief's 10-stage delivery process (brief §18). It answers
three questions at a glance: **where is each stage**, **where is each V1
module**, and **what's still open**. Update this file at the end of every
stage/remediation pass — it is the source of truth the HTML dashboard
(`docs/dashboard.html`, or the published Artifact) is generated from, not
the other way around.

**Last updated:** 2026-09-18 — **Module A (A0–A6) is complete.** `POST /sales` now has a real HTTP layer: `SaleController`/`SaleFinalizeRequest`, registered under `routes/web.php`'s `web` group, wires A4's `PosRequestContext` into the already-VERIFIED `CheckoutService::finalize()` exactly as `stage-6c-sale-finalization.md`'s "Authentication boundary" section specified — `terminal_id`/`cashier_id` are never read from the request body, proven directly. All nine of `module-a-auth-terminal-initialization.md` §16's Stage 6C unblock criteria are now met and demonstrated by passing tests; Stage 6C's own freeze as `stage-6c-baseline` remains an explicit owner decision, not self-sealed. Full detail in §22.

**How Module A got here**: A0 (baseline reconstruction) → A1 (login/logout/me, governance-sealed after two reconstructions for `VALIDATION_FAILED`) → A2 (Gate-based RBAC, no reconstruction needed) → A3 (terminal enrollment, ADR-011, governance-sealed after two further reconstructions for `ENROLLMENT_TOKEN_INVALID` and `TERMINAL_NOT_FOUND` — see "A3 baseline reconstruction" and "A3 second baseline reconstruction" below) → A4 (`PosRequestContext`/`ComposeAuthoritativeContext`, reusing `TERMINAL_NOT_ENROLLED` rather than a new code) → A5 (audited the full §13 test matrix, closed two real gaps) → A6 (this pass). Four baseline reconstructions total, all following the same reconstruct-don't-forward-edit discipline; a real project-wide PostgreSQL session-timezone bug was found and fixed along the way (A3). Full narrative detail in `docs/06-backend/module-a-auth-terminal-initialization.md` §17–§22.

### Stage 6C progress (2026-09-17, in progress — not frozen)

**Scope**: exactly one HTTP operation (`POST /sales`, `saleFinalize`), orchestrating ADR-003's 9-step transaction script by calling Stage 6A/6B's already-approved components (`FinancialCalculator`, `IdempotencyService`, `GlobalLockOrder`, `InvoiceSeriesAllocator`) — see `docs/06-backend/stage-6c-sale-finalization.md` for the complete operation inventory, invariant list, and evidence trail.

**What exists**: `app/Services/Checkout/CheckoutService.php` (full orchestration) plus three resolution helpers (`FiscalInstallationResolver`, `InventoryLocationResolver`, `TaxRegistrationResolver`, each with zero/one/many rigor — no `.first()` guessing), 9 new `DomainException`/resolution-exception classes, a new migration (`inventory_locations_one_default_per_store`, with a data-preflight abort check), and 34 new tests across `tests/Database/{ModelMassAssignmentRegressionTest,CheckoutResolversTest,CheckoutServiceTest,CheckoutServiceConcurrencyTest}.php` plus a dedicated `checkout_worker.php` for real multi-process concurrency proofs.

**Governance discipline maintained throughout**: two real defects were found in already-frozen Stage 5/6A files while integrating (`Sale`/`InvoiceSeries` `$fillable` gaps, an `ElectronicJournalEntry` ULID-into-`uuid`-column defect) — each fixed as the smallest possible forward correction with a dedicated regression test, disclosed in `stage-6c-sale-finalization.md`, with **no Stage 5/6A baseline retagged**. Five owner rulings were required and recorded (fiscal-installation resolution interval, default-location-only inventory deduction, dual-idempotency confirmation, the two model corrections, and Module A held out of scope) before implementation touched the corresponding code, plus a `transaction_number` format ruling (`T-` + ULID, distinct from `Sale.id`'s UUIDv7 and `Invoice.invoice_number`'s BIR serial) closed with three dedicated tests (shape, 10-worker concurrency stress, idempotent-replay consistency).

**Two independent readiness statuses, tracked separately so one is never mistaken for the other**:
- Checkout domain/service layer: **VERIFIED** (four gates closed in a dedicated second pass: transaction_number semantics, tax-registration resolution rigor, adversarial idempotency/atomicity proof, and full semantic — not just row-count — verification of every written table).
- `POST /sales` production readiness: **BLOCKED ON MODULE A** — no controller, `FormRequest`, or route exists yet, and none will until an authenticated/authorized terminal context exists to supply `CheckoutService`'s `$terminalId`/`$cashierId` from trusted state rather than caller-controlled request fields (this constraint is recorded in `stage-6c-sale-finalization.md` §7 for whoever builds that controller).

**Full regression at this point**: `tests/Unit` 85/196, `tests/Database` 130/466, `tests/Feature` 1/1, migration round-trip (`fresh`/`reset`/`migrate`) clean, Pint clean, `scripts/validate-baselines.sh` all 12 checks pass (no frozen baseline touched by any of this).

### Where the Stage 6B boundary actually is (and why this matters)

After the independent verification pass below, a follow-up commit added `scripts/validate-baselines.sh`, formalized the Stage Baseline Rule, and corrected a stale claim in `docs/06-backend/stage-6b-invoice-series-allocation.md`. That commit sat *after* the then-current `stage-6b-baseline` tag — which raised a real question: does "a stage baseline contains all approved content through that stage" mean all of that belongs inside the seal, or is some of it legitimately stage-neutral content that comes after?

The answer is **it was mixed, and treating it as one commit was itself the mistake.** Splitting by this reconstruction's own path-ownership map:

- **`docs/06-backend/stage-6b-invoice-series-allocation.md` is Stage 6B's own artifact** (it is literally named in the path-ownership map below). Its correction — fixing a "not yet retagged" claim that was no longer true — is Stage 6B content and belongs **inside** the seal. This was pulled forward into a new Stage 6B substantive commit, and `stage-6b-baseline` was moved to the manifest commit built on top of it.
- **`scripts/validate-baselines.sh` and the Stage Baseline Rule/verification narrative are not owned by any stage.** They document and enforce properties of the *whole reconstruction* (Stages 1 through 6B), not deliverable content of Stage 6B specifically — the same way `PROJECT-MANIFEST.md` itself has never been treated as any one stage's owned content. This is genuinely **post-boundary, stage-neutral governance**:

```text
stage-6b-baseline  (all approved Stage 1-6B content, including the corrected report)
      |
      v
this commit  (repository-wide governance: validator script, Stage Baseline Rule, this note)
      |
      v
Stage 6C work starts here (from main, not from checking out stage-6b-baseline directly)
```

**Consequence for Stage 6C**: work must branch from `main` (which carries this governance commit forward), never from `git checkout stage-6b-baseline` directly — checking out the tag alone would silently drop the validator script and the Stage Baseline Rule documentation from the tree Stage 6C builds on. `stage-6b-baseline` marks *what was approved*, not *where to start coding*.

### Independent verification pass (2026-09-17, after the linearization report)

The owner accepted the linearization's evidence but withheld final sign-off pending one independent re-verification, since the operation moved every baseline tag. Performed fresh, not by re-reading the prior report:

1. **Ancestry, re-run independently**: all six `git merge-base --is-ancestor` checks re-executed from scratch — all PASS.
2. **Tag resolution, re-verified**: all seven `stage-N-baseline^{commit}` lookups re-run — all resolve as expected.
3. **Isolation, re-run independently and extended to Stage 1** (the original report only checked the five *internal* boundaries — this pass added Stage 1, which the original omitted): all checks PASS, including a fixed false positive in the validator script written for this pass (see below).
4. **Content-equivalence, re-run independently for Stage 3/4/5/6A/6B** against their respective pre-linearization reference commits (`c8a8bbe`, `515e2db`, `f036da9`, `570dd75`, `228e033`): all five diffs empty.
5. **Stage 1 documentary basis — the owner's key question, answered from evidence, not inference.** The question was whether BIR-012/013/014 were *formally determined* to be Stage 1 amendments, not merely content that happened to land there. Evidence found: (a) the commit that introduced BIR-012/013 (`b5f270b`, a Stage-2-labeled commit) explicitly states in its own message and in the document text it added — *"Added BIR-012... and BIR-013... **to the compliance register**... informational only, **no Stage 2 domain behavior changed on this basis**"* — a contemporaneous, explicit disclaimer that this was a Stage 1 (compliance register) addition, not a Stage 2 domain change; (b) `bir-reference-register.md` has been classified as Stage 1 (`docs/01-research/`) content in every version of this manifest since the very first one; (c) the first manifest commit (`5cac9f1`, 17:46) was written *after* BIR-012/013 already existed (`b5f270b`, 13:25) and still classified the file purely as Stage 1's own deliverable, with no Stage 2 caveat — the classification is contemporaneous, not a retroactive judgment made during this reconstruction. Conclusion: the Stage 1 fix is sound.
6. **Stage 5 equivalence, explicitly captured** in the Git baseline history table below, matching the treatment already given to Stage 3/4/6A/6B.
7. **Full suite re-run from an independent worktree checkout of the tag itself** (`git worktree add`, detached HEAD at `stage-6b-baseline`, not the branch used to build the reconstruction): `migrate:fresh` clean, `tests/Unit` 85/196, `tests/Database` 98/305, `tests/Feature` 1/1, Pint clean — identical to every prior run. Worktree removed after verification.
8. **Git state**: `git status --porcelain` empty; only `main` remains.
9. **External/stale-reference sweep**: no CI/CD configuration exists in this repository, so no automated pipeline depends on any baseline hash. Searched all of `docs/` for every superseded hash. Found two categories: (a) `openapi.yaml`, `api-design.md`, and `domain-model.md` cite specific stage-baseline commits in their own status headers as "built against" provenance (e.g. `api-design.md` already cited `11d594f` for `stage-2-baseline`, stale by dozens of commits *before this session ever began*) — a pre-existing, project-wide point-in-time citation convention, left unchanged and flagged here rather than silently judged out of scope; (b) `docs/06-backend/stage-6b-invoice-series-allocation.md`'s own Status header said "Not yet retagged... all five still unmoved," which was no longer true and actively misrepresented the current state — **this one was fixed, and because that file is Stage 6B's own artifact, the fix was folded into the sealed Stage 6B boundary itself** (see "Where the Stage 6B boundary actually is" above), not left in this stage-neutral commit.
10. **Backup retained**: `backup/pre-final-baseline-reconcile` is kept, per the owner's explicit instruction, at least through the start of Stage 6C work.

### Permanent repository controls added

- **[`scripts/validate-baselines.sh`](../scripts/validate-baselines.sh)** — a read-only validator checking both required invariants (ordering + isolation) for every `stage-N-baseline` pair, driven by the same path-ownership map documented below. Run it after moving any baseline tag and before creating a new one. Building it caught one real bug in the map itself: `.gitignore` had been listed as Stage 5's own path, but it actually first exists at Stage 1 (a minimal file) and is only *expanded* at Stage 5 — a path that already exists is not "introduced" by a later stage merely because that stage edits its content, the same way `domain-model.md` stays Stage-2-owned across many amendment passes. Fixed in the script; documented here so the distinction isn't relitigated.
- **Stage Baseline Rule (formalized, binding from Stage 6C onward):** *A stage baseline is an immutable repository boundary containing all approved content through that stage and no content owned by a later stage. Work for Stage N+1 must branch or continue only from the canonical Stage N baseline (in practice: from `main`, which always carries the baseline forward plus any stage-neutral governance layered after it — see above). Amendments to an earlier stage must be formally classified (which stage actually owns the changed content — not which stage's work was in progress when it was written) and, where a canonical baseline must be reconstructed as a result, every downstream baseline must be regenerated and revalidated with `scripts/validate-baselines.sh`, not spot-checked.*
- **Commit convention (formalized):** each stage boundary from Stage 2 onward is exactly two commits — one substantive (the stage's own content) and one manifest/boundary commit recording verification evidence — with the `stage-N-baseline` tag always on the manifest commit. Stage-neutral governance content (tooling, cross-cutting rules, this manifest's own narrative sections) is never folded into a stage's substantive commit merely because of when it was written; it gets its own commit, explicitly identified as sitting after the relevant boundary, exactly as this commit does.

**Disposition: Stage 6B — PERMANENTLY SEALED.** All items in the owner's verification table are satisfied, including the boundary-placement question this commit itself resolves. Stage 6C (Sale Finalization) may now begin, but only on separate, explicit instruction — this manifest update does not itself authorize starting it.

---

### Linearization narrative (2026-09-17, prior to the independent verification pass above)

The owner rejected an earlier two-part reconciliation of this baseline chain because, although its file *content* was correct, its *ancestry* was not: the InvoiceSeries amendment had been inserted after the old Stage 5 boundary (`8c8fe32`), which itself sat downstream of the original Stage 3/4/5 commits — so `stage-2-baseline` still contained Stage 3/4/5 content in its own history. Verification showed this was not new: the project's original `stage-2-baseline` (`6da07bd`, then `b7ef8fd` after the NON_VAT amendment) had always had Stage 3's commit as a git ancestor, because every stage in this project's single-branch history was amended interleaved with later stages' own work. The owner's decision: fully linearize from Stage 1 forward so that for every adjacent pair, `stage-N-baseline` is a genuine git ancestor of `stage-(N+1)-baseline`, **and** no stage's baseline contains a single path owned by a later stage.

### Reconstruction method

Each stage boundary was built by checking out the exact, already-approved file content for that stage's own paths from the previously accepted tip (tagged `backup/pre-final-baseline-reconcile`) directly onto the *previous* stage's already-reconstructed boundary — `git checkout <tip> -- <stage-owned paths>` — rather than replaying the original interleaved commit sequence. This guarantees every commit forward is a strict superset of the one before it, so ancestry and isolation are structural, not merely reviewed. No content was re-authored, redesigned, or reworded: it was relocated in the graph. This was verified at the end by diffing the final tip against the backup and finding zero difference outside `PROJECT-MANIFEST.md` itself.

One genuine gap was found and fixed *during* this process, not assumed away: Stage 1's own tagged baseline (`ac907e1`) turned out not to be Stage 1's final approved content. `docs/01-research/bir-reference-register.md` had been amended (BIR-012/013/014 added, then upgraded to HIGH confidence) during the Stage 2 discount-allocation pass — a Stage-1-owned file amended out of stage order, same pattern as every other stage. This was caught by the same content-diff verification method being used everywhere else, fixed with a dedicated commit as a child of `ac907e1`, and the entire chain was rebased onto the corrected Stage 1 boundary (a clean, zero-conflict rebase, since nothing else touches that file).

### Path-ownership map used throughout

- **Stage 1**: `docs/00-product/`, `docs/01-research/`, `docs/06-ui/`
- **Stage 2**: `docs/02-domain/`, `docs/03-architecture/erd.md` (Stage 2 content per its own status header, despite its directory)
- **Stage 3**: `docs/03-architecture/architecture.md`, `deployment.md`, `offline-strategy.md`, `decisions/ADR-*.md`
- **Stage 4**: `docs/05-api/`
- **Stage 5**: `docs/04-database/`, `database/migrations/`, `database/factories/`, `database/seeders/`, `database/scripts/`, `app/Models/`, 10 named Stage-5-owned files in `tests/Database/` (including `TerminalCredentialHashUpgradeTest.php`, added by the A0 reconstruction to complete Ruling 3's credential_hash uniqueness intent), plus the Laravel application scaffold (first required here — Stages 1–4 are pure documentation)
- **Stage 6A**: `app/Domain/Exceptions/{Concurrency,Domain,FiscalDayClosed,IdempotencyKeyReused,InvalidTaxConfiguration,RefundExceedsRemainingAmount,RefundExceedsRemainingQuantity,SaleNotVoidable,ShiftNotOpen}Exception.php`, `app/Domain/Financial/`, `app/Domain/Money.php`, `app/Domain/Quantity.php`, `app/Services/Idempotency/`, `app/Support/`, `docs/06-backend/stage-6a-transaction-foundation.md`, 3 named files in `tests/Database/` + their worker, `tests/Unit/Domain/`, `tests/Unit/Services/CanonicalRequestHasherTest.php`, `tests/Unit/Support/`
- **Stage 6B**: `app/Domain/Exceptions/InvoiceSeries{Exhausted,Resolution}Exception.php`, `app/Services/InvoiceNumbering/`, `docs/06-backend/stage-6b-invoice-series-allocation.md`, 2 named files in `tests/Database/` + their worker, `tests/Unit/Services/InvoiceNumbering/`

---

### A0 baseline reconstruction (2026-09-17)

**Why this was required**: `docs/06-backend/module-a-auth-terminal-initialization.md` §14's Decision Register (CLOSED) approved two changes that modify content already sealed inside existing baselines rather than adding new, later-stage-owned files:

- **Ruling 3/6 (Stage 4)**: adding the `terminalCookieAuth` securityScheme, the conjunctive `cookieAuth`+`terminalCookieAuth` requirement on every POS_TERMINAL operation, and the `RATE_LIMITED` error-catalog code all edit the CONTENT of `docs/05-api/openapi.yaml`/`error-catalog.md` — files `stage-4-baseline` already froze. A forward commit plus a tag move would have inverted the required `stage-4-baseline → stage-5-baseline → ... → stage-6b-baseline` ancestry (confirmed via `git merge-base --is-ancestor` before starting), so every downstream baseline needed to be regenerated on top of the amended Stage 4 content, per the Stage Baseline Rule.
- **Ruling 3 (Stage 5)**: the `terminals.credential_hash` partial unique index completes an ADR-011 intent the Stage 5 `terminals` migration's own comment already declared, the same way the sibling `terminal_enrollment_tokens.token_hash` unique index (same Stage 5 pass) already does — a genuinely Stage-5-owned persistence invariant, not a new one, so it was folded into the same reconstruction's Stage 5 replay step rather than left as a standalone Stage 6C-style forward-fix.

**Method**: per the Decision Register's A0 sequence — preserve `main`, rebuild from the current `stage-4-baseline` forward by cherry-picking every stage's actual substantive commits (identified from `git log`, not assumed from prior prose) onto a scratch branch, applying the two amendments at their correct boundaries, validating at every step, then moving `main` and the four tags only after full validation.

**Old → new baseline mapping** (Stage 1/2/3 tags **unchanged**):

| Baseline | Old | New |
|---|---|---|
| `stage-4-baseline` | `2313a16` | `d8f05f9` |
| `stage-5-baseline` | `4c16ee0` | `d5fdf87` |
| `stage-6a-baseline` | `47fa84d` | `6250574` |
| `stage-6b-baseline` | `a667edc` | `f4601ca` |

**Key commits**:
- Preserved backup of pre-A0 `main` tip: `backup/pre-module-a-a0` @ `73587d1`
- Stage 4 amendment commit: `d8f05f9` (`feat(api): apply approved Stage 4 amendment (A0 step 4)`) — also the new `stage-4-baseline`
- Stage 5 credential-index amendment commit: `d5fdf87` (`feat(database): complete Stage 5's credential_hash uniqueness intent (A0 step 5)`) — also the new `stage-5-baseline`
- Reconstructed final `main` tip: `54a7e1a` (identical, commit-for-commit content, to the last of the 13 post-`stage-6b-baseline` commits that were on the preserved `main`, replayed onto the reconstructed chain)

**Verification performed**: `git diff backup/pre-module-a-a0 HEAD` shows content differences in exactly 6 files — the two Stage 4 contract files, the new Stage 5 migration/test pair, and two ownership-map sync lines (`scripts/validate-baselines.sh`, this manifest) — nothing else changed anywhere in the tree. All 6 ancestry checks and all applicable isolation checks pass (`scripts/validate-baselines.sh`, 12/12). Full regression (Unit 85/196, Database 139/489, Feature 1/1, Stage 6A/6B/6C concurrency suites, migration round-trip, Pint) is green on the reconstructed `main`. Full detail in the A0 completion report delivered alongside this update.

**A1 (Module A's own auth/session implementation) has NOT started.** This reconstruction is scope-limited to A0; no authentication, authorization, or terminal-enrollment code exists yet.

---

### A1 baseline reconstruction (2026-09-18)

**Why this was required**: during A1's sealing correction, `VALIDATION_FAILED` (422) was added to `docs/05-api/error-catalog.md` as an ordinary forward commit (`d38d6e4`), on the reasoning that it was new content rather than an edit to already-declared intent. The owner correctly identified this as the same class of frozen-contract change A0 already established requires full reconstruction, not a forward edit: `error-catalog.md` is Stage-4-owned content, and `stage-4-baseline` had already been reconstructed once (by A0) to contain the *complete, correct* Stage 4 contract — a second forward edit to that same file would leave `stage-4-baseline` stale again, for the identical structural reason (ancestry ordering) A0's own report explains.

**Governance check performed first, per instruction**: inspected the complete `error-catalog.md`, `components.schemas.Error`, and `components.responses.UnprocessableEntity` at the actual frozen `stage-4-baseline` (not `main`, which already carried the amendment under review) — no existing code semantically covers a `FormRequest`'s own structural/shape validation failure; every 422 code is either protocol-level or tied to a specific business field, and `CONCURRENCY_CONFLICT`'s "generic" scope is explicitly limited to 409 state races. Repurposing an existing code was therefore rejected, per instruction. `components.schemas.Error` itself needed no change to accommodate the new code: `details` is optional (`type: object, additionalProperties: true`, no value-type constraint) and `code`/`message`/`request_id` are unconstrained/required strings — the centralized `ValidationException` renderer's JSON was verified to already conform.

**Old → new baseline mapping** (values here are A0's *output*, i.e. what A1 started from; Stage 1/2/3 tags **unchanged** throughout both reconstructions):

| Baseline | Before (A0's output) | After (A1 reconstruction) |
|---|---|---|
| `stage-4-baseline` | `d8f05f9` | `f57e924` |
| `stage-5-baseline` | `d5fdf87` | `678d597` |
| `stage-6a-baseline` | `6250574` | `1c0e5c9` |
| `stage-6b-baseline` | `f4601ca` | `d50abd7` |

**Key commits**:
- Preserved backup of pre-reconstruction `main` tip: `backup/pre-a1-validation-failed-reconstruction` @ `d25e802`
- Stage 4 amendment commit: `f57e924` (`feat(api): add VALIDATION_FAILED to the Stage 4 error catalog (2nd reconstruction, step 4)`) — also the new `stage-4-baseline`
- Reconstructed final `main` tip: `ec95b4b` (content-identical to the preserved `main`'s tip, `d25e802`)

**Verification performed**: `git diff backup/pre-a1-validation-failed-reconstruction HEAD` is **completely empty** — not just "6 files," genuinely nothing — because `VALIDATION_FAILED` already existed in the preserved tree (as the forward commit under correction) and the reconstruction only moved *where in history* that line was introduced, never its final content. `git cherry-pick`ing the original forward-fix commit (`d38d6e4`) onto the amended Stage 4 boundary produced a 3-way-merge no-op for `error-catalog.md` alone (git recognized the target line was already present) while its other two files' changes applied normally — confirmed by `grep -c VALIDATION_FAILED` returning exactly `1`, not `2`. All 5 ancestry checks and all applicable isolation checks pass (`scripts/validate-baselines.sh`, 12/12). Full regression (Unit 89/209, Database 160/566, Feature 1/1, Stage 6A/6B/6C concurrency suites, migration round-trip, Pint) is green on the reconstructed `main`.

**A2 has NOT started.** Both `backup/pre-module-a-a0` and `backup/pre-a1-validation-failed-reconstruction` are retained.

---

### A3 baseline reconstruction (2026-09-18)

**Why this was required**: A3's implementation surfaced a genuine third Stage 4 gap — `terminalEnroll`'s `409` outcome ("token already used, expired, or revoked") referenced the generic `Error` envelope with no registered code. `ENROLLMENT_TOKEN_INVALID` was implemented as the working exception, but (per the owner's ruling) approving it as a real code means it belongs inside `error-catalog.md`'s CONTENT — Stage-4-owned, frozen — so it required the same reconstruct-don't-forward-edit discipline as A0 and the A1 `VALIDATION_FAILED` correction, not a new forward commit.

**Governance check performed first, per instruction**: inspected `error-catalog.md` at the actual frozen `stage-4-baseline` (not `main`) for a reusable existing code before treating this as a new amendment. `CONCURRENCY_CONFLICT` was the only plausible candidate and was rejected: its description is scoped to a current-state race, which fits "already used" but not "expired" or "wrong store," and the frozen contract's `terminalEnroll` `409` describes one non-enumerated outcome, not three-plus sub-cases to split across codes. No existing code fit, so this was confirmed as a genuine amendment.

**Two implementation corrections closed before reconstruction, so the chain would only need to move once**:
1. `terminalCurrent`'s production route removed from `routes/web.php`'s main group (a Store-A user holding a Store-B credential could otherwise observe that terminal before A4's coherence check exists) and re-registered only inside the existing `if (app()->environment('testing'))` block, at the identical path. The resolver and controller action remain fully implemented and tested.
2. Store-scoping for terminal management (`list`/`get`/`createEnrollmentToken`/`revoke`) made executable and tested, not just architecturally assumed — three dedicated tests, plus a `TerminalEnrollmentService::consumeToken()` hardening so a cross-store enrollment-token claim is rejected inside the same locked transaction before the token is consumed.

Full detail on both corrections, plus the failure-boundary and timezone-invariant tests added alongside them, is in `docs/06-backend/module-a-auth-terminal-initialization.md` §19a.

**Method**: identical to the A0/A1 reconstructions — preserve `main`, rebuild from `stage-4-baseline` forward on a scratch branch by cherry-picking every stage's actual substantive commits, apply the amendment at its correct boundary, validate ancestry and isolation at every step, run full regression, then move tags and `main` only after everything is green.

**Old → new baseline mapping** (values here are the A1 reconstruction's *output*, i.e. what A3 started from; Stage 1/2/3 tags **unchanged** throughout all three reconstructions):

| Baseline | Before (A1 reconstruction's output) | After (A3 reconstruction) |
|---|---|---|
| `stage-4-baseline` | `f57e924` | `776c847` |
| `stage-5-baseline` | `678d597` | `68aafb9` |
| `stage-6a-baseline` | `1c0e5c9` | `26c626e` |
| `stage-6b-baseline` | `d50abd7` | `5a24120` |

**Key commits**:
- A3 closeout commit (pre-reconstruction, on the old chain): `b98e576` (`fix(backend): A3 closeout -- defer terminalCurrent to A4, prove Store scoping`)
- Preserved backup of pre-reconstruction `main` tip: `backup/pre-a3-enrollment-token-reconstruction` @ `b98e576`
- Stage 4 amendment commit: `776c847` (`feat(api): add ENROLLMENT_TOKEN_INVALID to the Stage 4 error catalog (3rd reconstruction, step 11)`) — also the new `stage-4-baseline`
- Reconstructed final `main` tip: `6884e9f` (full: `6884e9ffa25af77f8e563ed537e0bf4faf2cf16a`)

**Verification performed**: `git diff --stat backup/pre-a3-enrollment-token-reconstruction HEAD` shows **exactly one line changed** — the new `ENROLLMENT_TOKEN_INVALID` row in `error-catalog.md` — **not an empty diff**, and this is the expected, correct result rather than a discrepancy. It differs from the A1 reconstruction's empty diff for a specific, deliberate reason: `VALIDATION_FAILED` had already been forward-committed to `main` before that reconstruction began, so the row already existed in the preserved tree and reconstruction only relocated *where in history* it was introduced. Applying that lesson, `ENROLLMENT_TOKEN_INVALID` was **not** forward-committed to `error-catalog.md` before this reconstruction — it existed only as the exception class's `errorCode()` return value — so the preserved backup tree genuinely lacked the catalog row, and this reconstruction is what introduces it, as a reviewed, owner-approved amendment. All 5 ancestry checks (`git merge-base --is-ancestor`, `stage-3-baseline` through the final tip) and all applicable isolation checks (`git ls-tree` at each of the four new boundaries) pass, both before and after the tag move (`scripts/validate-baselines.sh`, 12/12 each time). Full regression at the reconstructed tip, before any tag moved: fresh migration and migration round-trip (`fresh`/`reset`/`migrate`) clean; `tests/Unit` 102/474; `tests/Database` 194/668; `tests/Feature` 1/1; A1 suite 21/77; A2 suite (`AuthorizationTest` 5/12 + `Unit/Services/Auth` 17/278); A3 suite (`TerminalEnrollmentTest` 26/78 + `DatabaseConnectionTimezoneTest` 2/3); enrollment-token multiprocess concurrency 1/9; Stage 6A concurrency 3/33; Stage 6B concurrency 3/105; Stage 6C checkout 12/105 and checkout-concurrency 3/46; OpenAPI YAML parse clean (`Symfony\Component\Yaml\Yaml::parseFile()`); Pint clean.

**A4 has NOT started.** All prior backup tags (`backup/pre-module-a-a0`, `backup/pre-a1-validation-failed-reconstruction`, `backup/pre-final-baseline-reconcile`) plus the new `backup/pre-a3-enrollment-token-reconstruction` are retained. **This reconstruction's own A3-sealed declaration was itself held short**, pending one further review — see "A3 second baseline reconstruction" immediately below.

---

### A3 second baseline reconstruction (2026-09-18)

**Why this was required**: a review of the A3 closeout above accepted every item except one: the reported Store-scope tests (`terminalList` scoping, request-body override immunity, cross-store enrollment-token consumption) don't individually prove all four terminal-management operations are Store-scoped. Cross-store **token consumption** and cross-store **token issuance** are different attack paths — an administrator being unable to *use* another store's token says nothing about whether they can *generate* one for it in the first place — and `terminalGet`/`terminalRevoke` had no dedicated test beyond a single combined check that only asserted the HTTP status code, never the response body or side effects.

**Governance check performed first, per instruction, before writing any fix**: three new tests were added proving each operation individually with real side-effect assertions (`tests/Database/TerminalEnrollmentTest.php`: `test_cross_store_terminal_get_does_not_expose_store_b_terminal_information`, `test_cross_store_enrollment_token_issuance_fails_and_creates_no_row`, `test_cross_store_terminal_revoke_fails_and_leaves_store_b_terminal_unaffected`), replacing the shallow status-only test. Writing them surfaced a real, pre-existing defect (not introduced by this pass): `TerminalController::get()`/`createEnrollmentToken()`/`revoke()` all used a bare `Terminal::where('store_id', ...)->findOrFail(...)`; a cross-store ID correctly 404s, but the body was Laravel's own raw exception shape, not the frozen envelope, because `ModelNotFoundException` is unconditionally rewrapped into `NotFoundHttpException` by `Illuminate\Foundation\Exceptions\Handler::prepareException()` before any custom renderer for the original type could run — the same class of Laravel-internal rewrap A2 already documented for `AuthorizationException → AccessDeniedHttpException`, confirmed via a real HTTP round-trip, not reasoned from documentation.

**Decision made directly** (the owner delegated this specific technical call, with instruction to audit against project precedent and consult external prior art where internal evidence ran out): no existing `error-catalog.md` code fits "this terminal record does not exist for this actor" — every other 404 in the codebase is resource-specific and already catalogued (`PRODUCT_NOT_FOUND`, `SALE_NOT_FOUND`, `VOID_NOT_FOUND`, `REFUND_NOT_FOUND`, `INVOICE_NOT_FOUND`), with no generic fallback to reuse. `TERMINAL_NOT_FOUND` (404) follows that identical, already-five-times-established pattern. A brief check of UTAK's and StoreHub's public help centers returned no technical API/error-code material (both are customer support sites, not API references); a general search independently confirmed the already-implemented HTTP status choice (404, not 403, for a cross-tenant resource) matches standard multi-tenant REST practice, so that part required no change — only the envelope-conformance defect and the missing code did.

**Method**: identical to the prior three reconstructions. Application code (`TerminalNotFoundException`, the three controller methods, the three new tests) was committed first as ordinary application code, deliberately *without* touching `error-catalog.md` (commit `6c76799`), applying the lesson recorded above about the `ENROLLMENT_TOKEN_INVALID` workflow deviation. The catalog row was then added only at the Stage 4 boundary during reconstruction.

**Old → new baseline mapping** (values here are the first A3 reconstruction's *output*; Stage 1/2/3 tags **unchanged** throughout all four reconstructions):

| Baseline | Before (A3 1st-reconstruction output) | After (A3 2nd reconstruction) |
|---|---|---|
| `stage-4-baseline` | `776c847` | `c231a31` |
| `stage-5-baseline` | `68aafb9` | `b9f4d3f` |
| `stage-6a-baseline` | `26c626e` | `ba97dfc` |
| `stage-6b-baseline` | `5a24120` | `721cd72` |

**Key commits**:
- A3 final closeout commit (pre-reconstruction, on the old chain): `6c76799` (`fix(backend): A3 final closeout -- prove all four Store-management boundaries`)
- Preserved backup of pre-reconstruction `main` tip: `backup/pre-a3-terminal-not-found-reconstruction` @ `6c76799`
- Stage 4 amendment commit: `c231a31` (`feat(api): add TERMINAL_NOT_FOUND to the Stage 4 error catalog (4th reconstruction, step 1)`) — also the new `stage-4-baseline`
- Reconstructed final `main` tip: `ae6ffb2` (full: `ae6ffb2e377873f9650696f929f74e339dec9ac5`)

**Verification performed**: `git diff --stat backup/pre-a3-terminal-not-found-reconstruction HEAD` shows **exactly one line changed** — the new `TERMINAL_NOT_FOUND` row — matching the same deliberate (not forward-committed) pattern as `ENROLLMENT_TOKEN_INVALID`. All 5 ancestry checks and all applicable isolation checks pass, both before and after the tag move (`scripts/validate-baselines.sh`, 12/12 each time). Full regression at the reconstructed tip, before any tag moved: fresh migration and migration round-trip clean; `tests/Unit` 102/474; `tests/Database` 196/689; `tests/Feature` 1/1; A1 suite 21/77; A2 suite (`AuthorizationTest` 5/12 + `Unit/Services/Auth` 17/278); A3 suite (`TerminalEnrollmentTest` 28/99 + `DatabaseConnectionTimezoneTest` 2/3); enrollment-token multiprocess concurrency 1/9; Stage 6A/6B concurrency; Stage 6C checkout and checkout-concurrency; OpenAPI YAML parse clean; Pint clean. `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `main` force-updated to `ae6ffb2` (non-fast-forward, same as the prior three reconstructions).

**A4 has NOT started.** All five backup tags are retained (`backup/pre-module-a-a0`, `backup/pre-a1-validation-failed-reconstruction`, `backup/pre-final-baseline-reconcile`, `backup/pre-a3-enrollment-token-reconstruction`, `backup/pre-a3-terminal-not-found-reconstruction`). **A3 is now genuinely GOVERNANCE-SEALED.**

---

### Stage 6C freeze (2026-09-18)

**Why this is a new tag, not a reconstruction**: unlike A0/A1/A3's amendments, Stage 6C's completion (Module A's A6) added no content to any already-frozen Stage 1–6B file — `SaleController`/`SaleFinalizeRequest`/the Sale resources/`IdempotencyKeyRequiredException` are all genuinely new files, and `IDEMPOTENCY_KEY_REQUIRED` was already a frozen Stage 4 catalog entry with no prior implementing class. `stage-6c-baseline` is therefore a forward tag on `main`'s existing history, exactly like every prior stage's first freeze (Stage 1 through 6B) — no rebuild, no backup tag needed.

**Method**: (1) extended `scripts/validate-baselines.sh` — added `stage-6c-baseline` to the tracked `STAGES` array and a new `OWNERSHIP_stage_6c_baseline` pattern covering the original checkout domain/service-layer files (from the first Stage 6C implementation pass) plus A6's HTTP layer. Module A's own files (`AuthController`, `TerminalController`, middleware, `PosRequestContext`, etc.) are deliberately **not** claimed by this pattern — they are Module A's own cross-cutting initiative (tracked in `module-a-auth-terminal-initialization.md`), not Stage 6C content, even though A6 specifically unblocks Stage 6C. (2) Ran the validator with the new tag not yet created — confirmed `stage-6b-baseline` leaks nothing from the new `stage-6c` ownership pattern (expected: none of that content existed when `stage-6b-baseline` was tagged). (3) Confirmed by direct `git ls-tree` inspection that `main`'s current tip genuinely contains the Stage 6C-owned paths. (4) Updated `docs/06-backend/stage-6c-sale-finalization.md`'s own disposition line and this manifest's Stage tracker/At-a-glance/Frozen-corpus-rule sections to record the freeze. (5) Tagged `stage-6c-baseline` at the resulting commit (`de2a84c`). (6) Re-ran the validator with the tag now in place — all 14 checks pass (7 ordering, 7 isolation; `stage-6c-baseline` itself is `N/A` for isolation, being the last tracked stage).

**No reconstruction, no backup tag, no baseline moved.** Stages 1 through 6B are entirely untouched by this freeze.

---

### Stage 7 sitemap validation pass (2026-09-18)

**What changed**: `docs/06-ui/sitemap.md`'s two open questions (flagged in
the original Stage 1 draft as "proposed for validation in Stage 7") were
resolved directly in that document — a **forward edit to Stage-1-owned
content**, on explicit owner instruction given in the same turn as the
request, not a silent autonomous change. Resolution: `/back-office/
dashboard` is kept as a distinct route, and POS-mode transaction lookup/
void/refund reachability is confirmed — both grounded in targeted reference
research against UTAK's and StoreHub's public help centers (this project's
named competitor references). The route tree itself was not changed; no
domain, schema, or API-contract file was touched.

**Why no baseline reconstruction**: the Frozen-corpus rule's reconstruction
discipline (document → propose → STOP for approval; rebuild ancestry if a
baseline must move) exists to protect files with real downstream *code*
dependents (`openapi.yaml`, migrations, domain classes) where
`scripts/validate-baselines.sh`'s ordering/isolation invariants are
load-bearing. `sitemap.md` is narrative documentation with no code
dependent on its exact content — editing it does not invalidate any tagged
baseline's content-equivalence to anything else in the tree. `stage-1-
baseline` itself is unmoved; the edit is recorded as a dated revision-log
entry inside the document, the same pattern already used by `scope.md`'s
own post-freeze corrections.

**Not done, and why**: no competitor claim from this research (UTAK/
StoreHub) was added to `market-comparison.md` — that document is
product-positioning research with its own sourcing bar; this pass's
research was narrowly scoped to IA/navigation-pattern validation and is
cited only inside `sitemap.md` itself, with its own confidence/sourcing
note.

---

### Shift/FiscalDay backend (`shiftOpen`/`shiftCurrentGet`) and fifth baseline reconstruction (2026-09-18)

**Why this was built**: `docs/06-ui/stage-7-frontend-initialization.md` disclosed that a fresh store had no HTTP path to open a shift — `CheckoutService`'s own `OPEN` shift/fiscal_day precondition could only be satisfied by a factory/seeder, never a real request. This pass closes exactly that gap: `POST /shifts/open` (`ShiftOpenService`) atomically resolves-or-opens the terminal's `FiscalDay` before opening the `Shift` (api-design.md §10's V1 operating policy, not a BIR requirement), wrapped in `IdempotencyService` exactly like `CheckoutService`; `GET /shifts/current` lets the frontend check state before deciding whether to show an open-shift form. `terminal_id`/`cashier_id` are always resolved from `PosRequestContext` (A4), never the request body. Concurrency (invariants.md #32/#33/#34) is proven under two genuinely separate OS processes (`ShiftOpenConcurrencyTest`), not merely by the locking reasoning. `shiftGet`/`shiftList`, cash movements, X-Readings, `shiftClose`, and every `FiscalDay` operation besides the auto-open remain out of scope — `ShiftOpenResult` already returns everything the frontend needs to reach a POS checkout screen.

**A genuine frozen-contract gap, discovered and resolved directly** (a technical API-contract decision, not a business/legal one, per the standing autonomy directive — no owner escalation required): `openapi.yaml`'s `shiftCurrentGet` declares its "no open shift" case as a `404`, described inline as `SHIFT_NOT_OPEN` — but that name is already a distinct, frozen `409` code for a different scenario (a shift referenced by ID, found closed). Reusing it at `404` would violate `error-catalog.md`'s own "a given code's HTTP status never changes" rule; `SHIFT_REQUIRED` (409, the closest semantic match) is equally frozen at the wrong status. No existing code fits a `404` "no shift is currently open" outcome, so `NO_CURRENT_SHIFT` (404) is a genuine new Stage 4 amendment — added as application code first, deliberately without touching `error-catalog.md` in the same commit (`80c2061`), applying the lesson already learned from `ENROLLMENT_TOKEN_INVALID`/`TERMINAL_NOT_FOUND`.

**A second, unrelated bug found and fixed along the way**: the response's `fiscal_day_was_opened` flag was originally derived by comparing `shift.opened_at`/`fiscal_day.opened_at` timestamps — but both columns are `TIMESTAMPTZ(0)` (Stage 5's own whole-second precision), so two requests a few milliseconds apart can truncate to the same second and compare falsely equal. Found via a genuinely failing test, not by inspection; fixed by comparing which shift is earliest (by UUIDv7 `id`, time-sortable) for its `fiscal_day` instead — correct on both a fresh execution and an idempotent replay, with no extra column needed. A third, already-documented hazard (a bare `login()` call mid-test ambiently inheriting an earlier-captured session cookie — `module-a-auth-terminal-initialization.md` §20) recurred in one of the new HTTP tests and was fixed the same way as before.

**Fifth baseline reconstruction**: identical method to the prior four. Backup `backup/pre-shift-no-current-reconstruction` @ `80c2061`; rebuilt from `stage-4-baseline` forward on a scratch branch (`shift-no-current-reconstruction`); `NO_CURRENT_SHIFT` added to `error-catalog.md` at the new `stage-4-baseline` (`0a2967a`); Stage 5/6A/6B/6C and all 42 post-`stage-6b-baseline` commits replayed cleanly (no manual conflict resolution) to a final tip of `a902499`.

| Baseline | Before | After |
|---|---|---|
| `stage-4-baseline` | `c231a31` | `0a2967a` |
| `stage-5-baseline` | `b9f4d3f` | `a89a9ac` |
| `stage-6a-baseline` | `ba97dfc` | `ec0676b` |
| `stage-6b-baseline` | `721cd72` | `c1336c1` |
| `stage-6c-baseline` | `de2a84c` | `e75db55` |

**Verification**: `git diff --stat backup/pre-shift-no-current-reconstruction HEAD` shows exactly **one line changed** — the new `NO_CURRENT_SHIFT` row — matching the deliberate not-forward-committed pattern. All 7 ancestry checks (`stage-3-baseline` through the final tip) and all applicable isolation checks pass, both before and after the tag move (`scripts/validate-baselines.sh`, 14/14 each time — the new `stage-6c-baseline` boundary correctly contains `CheckoutService`/`SaleController` but not yet any Shift-service file). Full regression at the reconstructed tip, before any tag moved: fresh migration and round-trip clean; `tests/Unit` 102/474; `tests/Database` 221/785 (A1/A2/A3 suites, `ShiftOpenHttpTest`, `ShiftOpenConcurrencyTest`, `SaleFinalizationHttpTest`, Stage 6A/6B/6C concurrency individually re-verified); `tests/Feature` 1/1; OpenAPI YAML parse clean; Pint clean. All five `stage-*-baseline` tags moved only after all of the above passed; `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `main` force-updated to `a902499`; scratch branch deleted after the move. All six backup tags retained (`backup/pre-module-a-a0`, `backup/pre-a1-validation-failed-reconstruction`, `backup/pre-final-baseline-reconcile`, `backup/pre-a3-enrollment-token-reconstruction`, `backup/pre-a3-terminal-not-found-reconstruction`, `backup/pre-shift-no-current-reconstruction`).

---

### POS checkout screen — Stage 7 pass 2 (2026-09-18)

**Why this was built**: `shiftOpen`/`shiftCurrentGet` closed the last backend gap blocking a real POS checkout flow (see the Shift/FiscalDay entry above); this pass builds and verifies the screen itself. Two smaller gaps surfaced and were closed along the way, both using the same smallest-slice discipline applied throughout this project: no UI existed yet for A3's terminal-enrollment backend (added `resources/js/pages/TerminalEnroll.jsx`, TERMINAL_MANAGE-gated), and no Catalog backend existed at all for the cart to search against (added `GET /products` — `ProductController::list` + `ProductResource` — store-scoped, session-only per `operation-inventory.md`'s classification of `productList`, no terminal credential, no capability gate; `productCreate`/`productGet`/etc. remain deliberately out of scope).

`resources/js/pages/Pos.jsx` is a single `/pos` route with internal step state (`loading | not-enrolled | open-shift | cart | checkout | receipt`) rather than `sitemap.md`'s proposed separate `/pos/checkout` route — a deliberate implementation-simplicity deviation, since `sitemap.md` already documents its own IA as non-final. Every total shown before the receipt step is an explicitly labeled client-side preview only; `CheckoutService` always recomputes authoritatively server-side.

**Verified end-to-end in a real browser**, not just reviewed: login → enroll a terminal → open a shift → search/add a product → checkout (CASH) → receipt with a real `transaction_number`, sequential `invoice_number`s (`000001`, `000002`, `000003` across three separate sales in the same shift), correct `grand_total`/`amount_tendered`/`change` (including a ₱100-tendered-against-₱55-total case returning ₱45 change), and correct line items; "New sale" resets cart state without re-prompting for a shift.

**Disclosed, not fixed**: a genuinely fresh store cannot check out until an admin has configured `FiscalInstallation`, `InvoiceSeries`, a `terminal_fiscal_installations` mapping, `InventoryLocation`, and `tax_registrations` — confirmed directly when a freshly seeded store hit `FiscalInstallationResolutionException::noMappingForTerminal` during verification. That exception is deliberately NOT a `DomainException` (A6's design), so it correctly surfaces as a 500 rather than a stable client-facing code. No back-office UI/API exists yet for a store to self-serve this configuration — that remains Module B/C backend work. Full detail in `docs/06-ui/stage-7-frontend-initialization.md` §7.

No frozen-corpus baseline touched (new surface area only — `GET /products` — not a change to any already-frozen contract row). Full regression: Unit 102 + Feature 1 + Database 226 (incl. 5 new `ProductListHttpTest` cases) = 329 passing, 0 failures. Committed as `d713ecd`.

---

### Store setup & fiscal configuration — Stage 8, backend + admin screens (2026-09-18)

**Why this was built**: the Stage 7 pass 2 POS screen's own browser verification hit `FiscalInstallationResolutionException::noMappingForTerminal` — a fresh store had no HTTP path to configure any of the four checkout-time prerequisites (`FiscalInstallation`, `InvoiceSeries`, `InventoryLocation`, `TaxRegistration`), only factory/tinker seeding. This pass closes that gap with real admin CRUD and screens. Full detail in `docs/06-ui/stage-8-store-setup.md`.

**A genuinely new frozen-corpus situation, resolved by direct owner decision (not assumed)**: every operation built in this project so far already had a full or partial draft in `openapi.yaml` from Stage 1 onward. `InvoiceSeries`, `InventoryLocation`, and `fiscalInstallationAssignTerminal` had none — completing them "by the book" (matching all five prior reconstructions) would mean reconstructing from `stage-1-baseline` forward, the largest reconstruction this project would have done. Asked directly, the owner chose to **forward-commit the new `openapi.yaml` paths/schemas and five new `error-catalog.md` codes** (`FISCAL_INSTALLATION_NOT_FOUND`, `INVOICE_SERIES_NOT_FOUND`, `INVOICE_SERIES_ALREADY_ACTIVE`, `INVOICE_SERIES_ALREADY_CLOSED`, `INVENTORY_LOCATION_NOT_FOUND`) past `stage-6c-baseline`, without moving or reconstructing any of the 8 existing tags. `fiscalInstallationGet` was deliberately left unimplemented — completing its already-frozen-but-gapped `404` would be the old-style reconstruction case, out of scope for this decision. `scripts/validate-baselines.sh` still passes 14/14; no tag moved. `error-catalog.md` carries its own dated governance note recording this explicitly as a one-time extension of forward-commit governance to that file, distinct from completing a gap in already-frozen content.

**Admin surface** (`FISCAL_CONFIGURATION_MANAGE`, the existing Stage-4 capability, reused for all four entities — no new capability minted): FiscalInstallation List/Create (already-frozen, implemented for the first time) + AssignTerminal (new — the exact row `FiscalInstallationResolver` reads, reassignment closes the prior current mapping); TaxRegistration List/Create (already-frozen, implemented for the first time, "prior one closed the day before" per the resolver's inclusive-both-ends read); InvoiceSeries Create/Close (new — bootstraps `current_number = starting_number - 1`, at most one ACTIVE per installation, Create conflicts rather than auto-superseding); InventoryLocation Create/Update (new — first location auto-defaults, `is_default` may only be submitted `true`, never explicitly demoted). Plus a new read-only, terminal-scoped, no-capability `GET /store-setup/readiness` — independent re-implementations of each resolver's own lookup, never a call into the Stage-6C-frozen `CheckoutService` package.

**Frontend**: five new admin screens under `/admin/store-setup*` (`resources/js/pages/admin/`) in a dedicated dark theme scoped to this section only (POS/login/dashboard keep their existing light theme) — visual direction taken from the Stitch design-reference project the user supplied (`stitch.withgoogle.com/projects/13465419248509126107`), but every field grounded in the real Stage 5 schema; that mockup's invented fields (cryptographic key rotation, a hardcoded PH RDO dropdown, manager-PIN authorization, "SEQUENCE IMMUTABILITY PROTOCOL") were deliberately not built, per the standing rule against inventing unsupported business rules. `Pos.jsx` gained a `setup-incomplete` step that calls the new readiness endpoint before showing the cart.

**Verified end-to-end in a real browser**: created a second FiscalInstallation and InventoryLocation, reassigned the enrolled terminal to the new (unconfigured) installation and confirmed `/pos` correctly showed the blocked screen naming the exact missing prerequisite instead of the old raw 500; reassigned back and confirmed a real sale completed (invoice `#000004`, sequence continuing correctly); promoted a new default inventory location and confirmed the old one demoted; triggered `INVOICE_SERIES_ALREADY_ACTIVE` directly.

**Bug found and fixed during this verification**: `FiscalInstallation.terminals()` returns every terminal ever historically mapped, not just the current one — the admin screen was showing a just-superseded terminal as still assigned to the old installation (an admin-visibility bug; `FiscalInstallationResolver` itself was never affected, since it already filters by `effective_to` independently). Fixed with `wherePivotNull('effective_to')` on the eager-loaded relation in both call sites; regression test added.

No frozen baseline moved. Full regression: Unit 102 + Feature 1 + Database 256 (30 new store-setup tests) = 359 passing, 0 failures. Pint clean.

---

### Shift close / cash movements / X-Readings / FiscalDay close and sixth baseline reconstruction (2026-09-19)

**Why this was built**: `shiftOpen`/`shiftCurrentGet` (the fifth reconstruction, above) let a cashier start a day but never end one — `shiftClose`, `shiftCashMovementCreate`, `shiftXReadingList`/`Create`, and `fiscalDayClose`/`fiscalDayZReadingGet` were all already fully specified in the frozen Stage 4 contract (unlike the store-setup pass's `InvoiceSeries`/`InventoryLocation`) but had zero service or controller layer. This pass closes the lifecycle. Full detail in `docs/06-backend/stage-9-shift-close-fiscal-day-close.md`.

**Sixth reconstruction, back to the same forward-commit-vs-reconstruct question the store-setup pass raised — resolved by applying the rule already written down, not re-asking**: `shiftClose`/`shiftCashMovementCreate`/`shiftXReadingCreate`/`shiftXReadingList`/`fiscalDayClose`/`fiscalDayZReadingGet` all already declare a generic `404 NotFound` response in the Stage-4-frozen contract, with no specific code registered — the same shape as `TERMINAL_NOT_FOUND` and `NO_CURRENT_SHIFT` before it, and explicitly the *other* category error-catalog.md's own 2026-09-18 governance note calls out as still requiring the full reconstruction discipline (as opposed to the store-setup pass's wholly-new-operation forward commits). Two new codes were added at the `stage-4-baseline` boundary: `SHIFT_NOT_FOUND` (404) and `FISCAL_DAY_NOT_FOUND` (404, deliberately non-enumerating between "doesn't exist" and "exists but hasn't closed yet" for `fiscalDayZReadingGet`). `SHIFT_ALREADY_CLOSED` and `FISCAL_DAY_HAS_OPEN_SHIFT` needed no catalog change at all — both were already registered Stage 4 codes with no exception class or call site built yet.

**Reconstruction method**: identical to the prior five. Backup `backup/pre-shift-close-fiscal-day-close-reconstruction` @ `5b51cfc`; scratch branch `shift-close-fiscal-day-close-reconstruction` from `stage-4-baseline`; the two new rows added as a single commit (`9fdc2ca`) at that boundary; all 55 post-`stage-4-baseline` commits replayed cleanly via `git rebase --onto` (no manual conflict resolution) onto a `reconstruction-target` branch; verified each of the four downstream tags' new position via an empty `git diff <old-tag> <mapped-new-commit> -- . ':!docs/05-api/error-catalog.md'` before moving anything.

| Baseline | Before | After |
|---|---|---|
| `stage-4-baseline` | `0a2967a` | `9fdc2ca` |
| `stage-5-baseline` | `a89a9ac` | `71693bc` |
| `stage-6a-baseline` | `ec0676b` | `6591168` |
| `stage-6b-baseline` | `c1336c1` | `48fbbb6` |
| `stage-6c-baseline` | `e75db55` | `822455a` |

**Verification**: `git diff --stat backup/pre-shift-close-fiscal-day-close-reconstruction HEAD` shows exactly **one file, two lines changed** — the two new rows — matching the not-forward-committed pattern. All 14 `scripts/validate-baselines.sh` checks pass at the reconstructed tip, tested by moving the real tags directly (not a parallel test copy) since the backup tag makes that safe to undo. Full regression before `main` moved: `tests/Unit` 102/489 assertions, `tests/Database` 256/874 assertions, both unchanged from pre-reconstruction (no application code differs, only `error-catalog.md`'s two new rows). `main` force-updated to `cbe53d4`; `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; scratch branches deleted after the move. All seven backup tags retained.

---

### Reports module — Stage 10, backend only (2026-09-19)

**Why this was built**: named directly in the "Next action" row above after Stage 9 — `shiftGet`/`shiftList`/`fiscalDayGet`/`fiscalDayList` and the whole Reports tag were deferred there as "a natural fit for a future Reports module." Full detail in `docs/06-backend/stage-10-reports.md`.

**The first module needing zero frozen-corpus changes**: every one of the 15 Reports operations declares only `401`/`403` in its frozen response set — no `404`/`409`/`422` surface exists for any of them, confirmed by direct read of every operation block, and `operation-inventory.md` says so explicitly ("reports never fail on business state, only on auth"). Both statuses already map to long-registered codes. Built entirely as a forward addition — `scripts/validate-baselines.sh` stayed 14/14 throughout, no tag touched, no `error-catalog.md` row added.

**Architecture**: `ReportQueryService` (15 read-only aggregate methods over `DB::table()`), a shared `RendersReportResponse` trait handling `Accept: text/csv` (exact column order per `docs/05-api/csv-export-contract.md`) vs `ReportResult` JSON content negotiation, and four thin controllers grouped by data source (`SalesReportController` ×8, `VoidRefundReportController` ×2, `InventoryReportController` ×3, `ShiftReportController` ×2) — all gated by the single already-existing `REPORT_VIEW` capability, session-only, no terminal credential.

**Aggregation convention established**: a `VOIDED` sale is excluded from every revenue-shaped total (matching Stage 9's Z-Reading precedent), while `REFUNDED`/`PARTIALLY_REFUNDED` sales are **not** excluded — the original transaction still happened and is still revenue, with the refund tracked as its own separate figure. `reportSalesByDateRange`/`reportVoids`/`reportRefunds` are raw listings that deliberately show every status instead, since that visibility is each report's own point.

Three new model factories added along the way for models that had none yet (`Category`, `StockMovement`, `StockBalance`), same pattern as Stage 8's `TaxRegistration`/`TerminalFiscalInstallation`.

No frozen baseline moved; no frontend built (explicitly scoped backend-only). Full regression: Unit 102 + Feature 1 + Database 301 (20 new Reports tests) = 404 passing, 0 failures. Pint clean.

---

### Reports frontend — Stage 11 (2026-09-19)

Admin (back-office) screens for the 15 Stage 10 reports: `/admin/reports` hub and `/admin/reports/:slug`, one schema-driven viewer fed by `reportsRegistry.js`, gated by `REPORT_VIEW` (ADMIN/MANAGER; not part of POS checkout). Built against the Stitch project's report mockups (8 of 15 reports have a dedicated mockup; the rest use the shared template; the six missing states — load errors, export states, entity filter, voids/refunds outcomes, low-stock triage, tablet/phone — were later generated and implemented, together with a sidebar/rail/drawer admin shell) — the full inventory of what exists, what is missing, and every deliberate deviation is in `docs/06-ui/stage-11-reports-frontend.md`. The spec's invented fields and endpoints were not built; the frontend follows the real API and the frozen CSV contract. One additive backend change: `status`/`requested_at` on void/refund report **JSON** rows only (CSV columns unchanged), because a rejected void was otherwise indistinguishable from an executed one. No frozen baseline touched. Verified in a real browser (figures cross-checked against the Stage 9 Z-Reading and shift close). Full regression: Unit 102 + Feature 1 + Database 302 = 405 passing.

---

### Catalog management — Stage 12 (2026-09-19)

Products, categories and brands: `productGet/Create/Update/Activate/Deactivate` and `categoryList/Create`, `brandList/Create` on the backend, plus `/admin/catalog/{products,categories,brands}` screens (slide-over product form, confirm-to-deactivate, inline category/brand creation). Full detail in `docs/06-backend/stage-12-catalog.md`.

**The second module needing zero frozen-corpus changes**: all of these operations were already in `openapi.yaml`, and `PRODUCT_NOT_FOUND` / `VALIDATION_FAILED` already cover their failures (a duplicate SKU or barcode is a field error, not a new code). No baseline tag moved, no `error-catalog.md` row added, `validate-baselines.sh` all invariants hold.

**A latent defect fixed**: checkout fetched products with no store scope, so a cashier could sell another store's product by id; it now filters on the terminal's store, with a regression test verified to fail without the fix. Also: display ids now use the random tail of a UUID (their prefix is a timestamp), in the reports and the catalog.

Full regression: Unit 102 + Feature 1 + Database 329 (27 new) = 432 passing. Pint clean. Verified in a real browser at desktop and phone widths. Not built: product CSV import/export, barcode lookup, category/brand rename or delete (no contract operation), Users management.

---

### Users management — Stage 13 (2026-09-19)

The five `openapi.yaml` Users operations plus a new `userActivate`, and a `/admin/users` screen (slide-over form, confirm-to-deactivate, generated passwords, role/status filters). Full detail in `docs/06-backend/stage-13-users.md`.

**Two owner decisions, both recorded here**: (1) `userDeactivate` had no inverse, so **`userActivate` was added** as a wholly new forward-committed operation; (2) `USER_NOT_FOUND` fills a gap in the already-frozen `userGet`/`userUpdate`/`userDeactivate` `404`s, which by the strict rule means reconstruction — the owner **approved forward-committing it instead**, as a one-time exception, because the repository is now published and a reconstruction would force-push `main` and the baseline tags. No tag moved; the governance exception is written into `error-catalog.md`. The next code that fills a gap in an already-frozen response reopens the question.

**Lock-out protection is partial by the owner's choice**: the API refuses only demoting the last active ADMIN (a `422` field error the contract already allows); the screen hides Deactivate and role change for your own account and the only active administrator. The API can still deactivate the last admin, and `AdminUserSeeder` does not recreate an admin while any ADMIN row exists — documented, not fixed.

Also extracted a shared `SlideOver` component (products and users use it). Full regression: Unit 102 + Feature 1 + Database 346 (17 new) = 449 passing. Pint clean. Verified in a real browser at desktop and phone widths.

---

### Stock receipts and adjustments — Stage 14 (2026-09-19)

All five `openapi.yaml` Inventory operations (`inventoryStockList`, `inventoryLowStockList`, `inventoryMovementList`, `inventoryReceiptCreate`, `inventoryAdjustmentCreate`) and `/admin/inventory/{stock,movements}` screens with Receive / Adjust slide-overs. Full detail in `docs/06-backend/stage-14-stock.md`.

**The third module needing zero frozen-corpus changes**: every failure maps onto an existing code (`STOCK_ADJUSTMENT_REASON_REQUIRED`, `PRODUCT_NOT_FOUND`, `IDEMPOTENCY_KEY_REQUIRED`/`REUSED`, `VALIDATION_FAILED`); no tag moved, `validate-baselines.sh` holds.

**A latent defect fixed**: checkout recorded `SALE` movements but never updated `stock_balances`, breaking invariant #44 from the first sale. `StockLedger` is now the sole writer of movements and balances (atomic upsert), checkout and the new operations both use it, and `php artisan inventory:rebuild-balances [--dry-run]` repairs older data (run on the dev database). Also fixed a 500 on `GET /products?category_id=<non-uuid>`.

**Decisions**: negative stock is allowed and warned about (no frozen "insufficient stock" code; sales must not be blocked by a wrong count); writes go to the default location (Stage 6C ruling), so the screen offers actions only on those rows; products that do not track inventory are excluded from the stock list, low-stock list and the on-hand report; every write creates an audit event and journal entry.

Full regression: Unit 102 + Feature 1 + Database 372 (26 new) = 475 passing. Pint clean. Verified in a real browser at desktop and phone widths.

---

### Sales history, void and refund — Stage 15 (2026-09-19)

The eleven `openapi.yaml` Sales-tag operations that had no code (`saleList`, `saleGet`, `saleVoid`, `saleRefund`, `voidList/Get/Approve/Reject`, `refundList/Get/Approve/Reject`) and the `/admin/sales`, `/admin/sales/:saleId` and `/admin/sales/approvals` screens. Full detail in `docs/06-backend/stage-15-sales-history-void-refund.md`.

**The fourth module needing zero frozen-corpus changes**: every failure already had a registered code (including `SALE_NOT_FOUND`, `VOID_NOT_FOUND`, `REFUND_NOT_FOUND`); no tag moved, `validate-baselines.sh` holds.

**Behaviour**: a user holding the approve capability executes in the same call, anyone else files a request a manager approves; approval is the fiscal execution point and is attributed to the *approver's* terminal, open shift and open fiscal day (never the requester's or the sale's). A refused execution rolls back completely and leaves the record `REQUESTED` with its idempotency key reusable. Refund amounts are never accepted from the client: they derive from each line's frozen `net_line_amount` with the cumulative-recompute rule and are re-derived at approval against refunds completed since. Stock comes back only for `RETURN_TO_STOCK` lines (and for a whole void), at the location the sale took it from. Two-process race tests prove a void and a refund can never both take effect and two approvals can never over-refund a line.

**Two documented gaps in the frozen text, resolved without touching it**: (1) a pending refund's lines cannot be rows (invariant #72 forbids settlement rows before `COMPLETED`), so they are kept on the immutable `REFUND_REQUESTED` audit event; (2) `voidReject`/`refundReject` need an `Idempotency-Key` but no terminal, while idempotency is scoped by terminal — the key is still required and a retry is made safe by state (the identical rejection replays; anything else is `…_NOT_PENDING_APPROVAL`). Also `VoidSummary.sale_id` is emitted although the schema omits it, so the approvals queue can name the sale.

Full regression: Unit 111 + Feature 1 + Database 421 (49 new) = 533 passing. Pint clean. Verified in a real browser at desktop and phone widths.

---

### Audit log and electronic journal — Stage 16 (2026-09-19)

`auditEventList` and `journalEntryList` (the latter with the pinned CSV export) and `/admin/audit` and `/admin/journal` screens, under a new Records sidebar section. Full detail in `docs/06-backend/stage-16-audit-journal.md`.

**Zero frozen-corpus changes, and one deliberately open question**: `auditEventGet` and `journalEntryGet` are **not built**. Each declares a `404` with no registered error code, so registering `AUDIT_EVENT_NOT_FOUND`/`JOURNAL_ENTRY_NOT_FOUND` would complete a gap in an already-frozen response — exactly the case the Stage 13 exception said reopens the reconstruction question. It is the owner's call (reconstruct and force-push `main` and the tags; forward-commit as a second recorded exception; or leave the two operations unbuilt). Nothing depends on them: a list row already carries the whole event.

Both logs are read-only (no write routes) and store-scoped. The journal CSV exports the whole filtered set, streamed from a cursor, in the exact column layout of `csv-export-contract.md`. Full regression: Unit 111 + Feature 1 + Database 433 (12 new) = 545 passing. Pint clean. The two screens were not driven in a browser by the implementer (no signed-in session was available); a dashboard button-row overflow found from an owner screenshot was fixed.

---

### Invoice view and reprint — Stage 17 (2026-09-19)

`invoiceGet` and `invoiceReprint` and an **Invoice** panel on the sale page. Full detail in `docs/06-backend/stage-17-invoice-reprint.md`.

**Zero frozen-corpus changes and no missing-code question** (`INVOICE_NOT_FOUND` is registered). Rendering is a pure function of the immutable snapshot, dispatched on `schema_version` behind the ADR-007 `InvoicePrinter` seam: an 80mm HTML document, every typed value escaped, no script. A reprint carries a visible `REPRINT — COPY` mark, allocates nothing and touches no fiscal record (tests compare the rows before and after), and records one `INVOICE_REPRINTED` audit event — its id is the `reprint_event_id` — plus a journal entry. Idempotent per (terminal, key): a replay returns the same occurrence byte for byte.

**One gap in the frozen text, resolved without touching it**: `ElectronicJournalEventType` is closed and has no reprint member, so a reprint is journalled as an `INVOICE` entry whose source is the reprint occurrence (`source_type = invoice_reprint`, `payload_json.is_reprint`), leaving the original invoice's entry untouched. **One gap disclosed, not fixed**: the snapshot checkout stores (flat version 1) has no `payments` block and none of the PTU/accreditation fields ADR-006's example lists, and seller name/TIN/address print blank while store settings are unset (no settings screen yet); a snapshot version 2 with its own renderer is the mechanism, but it should wait for the BIR field review (BIR-006) rather than be guessed.

Full regression: Unit 120 + Feature 1 + Database 444 (11 new) = 565 passing. Pint clean. The renderer output was checked in a real browser (layout, sandbox, a planted script not running); the sale-page panel and the print dialog were not driven end to end.

---

### Store settings — Stage 18 (2026-09-19)

`storeSettingsGet` and `storeSettingsUpdate` (Module B) and a **Store Setup → Business Details** screen. Full detail in `docs/06-backend/stage-18-store-settings.md`.

**Zero frozen-corpus changes and no missing-code question** (neither operation declares a `404`; `SETTINGS_CHANGED` is in the frozen event catalog). One row per store, created on the first save: a store without settings reads as a blank baseline and the first `PATCH` must supply `business_name`, `registered_name` and `tin`; later saves are true partial updates, audited with only the fields that changed. ADMIN only. No TIN format is imposed (BIR-006 open).

**Effect on invoices**: a change reaches invoices issued afterwards and never ones already issued (a test changes the settings twice and shows existing invoices render identically). To make the header, footer and branch code mean something, checkout now records `seller_branch_code`, `invoice_header` and `invoice_footer` in the snapshot as **optional, additive keys of schema version 1** and the renderer prints them when present; earlier invoices lack the keys and print nothing extra, so no version bump was needed. This fills the seller block that Stage 17 found blank.

Full regression: Unit 123 + Feature 1 + Database 457 (16 new) = 581 passing. Pint clean. The screen was not driven in a browser (no signed-in session was available).

---

## At a glance

| | |
|---|---|
| **Current stage** | **Stage 6 — COMPLETE and FROZEN** (6A/6B/6C, `stage-6c-baseline`). **Stage 7 — Frontend — IN PROGRESS**: pass 1 (React SPA scaffold, real login + authenticated landing) done; pass 2 (POS checkout screen) done. **Stage 8 — Store Setup & Fiscal Configuration — DONE**: FiscalInstallation/InvoiceSeries/InventoryLocation/TaxRegistration admin CRUD + 5 admin screens + readiness pre-check, verified end-to-end. **Stage 9 — Shift Close / Cash Movements / X-Readings / FiscalDay Close — DONE**: completes the Shift/FiscalDay lifecycle, verified end-to-end (real variance calculation, real Z-Reading generation). **Stage 10 — Reports Module — DONE (backend only)**: all 15 `openapi.yaml` Reports operations, JSON + CSV export, zero frozen-corpus changes needed. **Stage 11 — Reports Frontend — DONE**: `/admin/reports` hub + a schema-driven viewer for all 15 reports (JSON + CSV export, inline 403), verified in a real browser; see `docs/06-ui/stage-11-reports-frontend.md` for the Stitch mockup inventory and deliberate deviations. **Stage 12 — Catalog management — DONE**: products/categories/brands backend and admin screens, zero frozen-corpus changes; see `docs/06-backend/stage-12-catalog.md`. **Stage 13 — Users management — DONE**: five Users operations plus the new `userActivate`, admin screen, two owner-approved contract decisions (see its own section); **Stage 14 — Stock receipts/adjustments — DONE**: all five Inventory operations, Stock and Movements screens, checkout's stock-balance defect fixed, zero frozen-corpus changes (see its own section); **Stage 15 — Sales history, void and refund — DONE**: `saleList`/`saleGet`/`saleVoid`/`saleRefund` and the void and refund approval workflow, three screens, zero frozen-corpus changes (see its own section); **Stage 16 — Audit log and electronic journal — DONE (lists only)**: `auditEventList`/`journalEntryList` with CSV export and two screens; the two `Get` operations wait on an owner decision (see its own section); **Stage 17 — Invoice view and reprint — DONE**: `invoiceGet`/`invoiceReprint` and an Invoice panel, zero frozen-corpus changes (see its own section); **Stage 18 — Store settings — DONE**: `storeSettingsGet`/`storeSettingsUpdate` and a Business Details screen, zero frozen-corpus changes (see its own section); `shiftGet`/`shiftList`/`fiscalDayGet`/`fiscalDayList`/`fiscalDayCurrentGet` remain unbuilt. |
| **Frozen baselines** | Stage 1 (`stage-1-baseline` @ `d93a813`, unchanged), Stage 2 (`stage-2-baseline` @ `5a7382a`, unchanged), Stage 3 (`stage-3-baseline` @ `78a190c`, unchanged), Stage 4 (`stage-4-baseline` @ `9fdc2ca` — **reconstructed six times**, A0, then the A1 `VALIDATION_FAILED` correction, then the A3 `ENROLLMENT_TOKEN_INVALID` correction, then the A3 `TERMINAL_NOT_FOUND` correction, then the `NO_CURRENT_SHIFT` correction, then the `SHIFT_NOT_FOUND`/`FISCAL_DAY_NOT_FOUND` correction; originally `2313a16`), Stage 5 (`stage-5-baseline` @ `71693bc` — reconstructed six times, was `4c16ee0`), Stage 6A (`stage-6a-baseline` @ `6591168` — reconstructed six times, was `47fa84d`), Stage 6B (`stage-6b-baseline` @ `48fbbb6` — reconstructed six times, was `a667edc`), Stage 6C (`stage-6c-baseline` @ `822455a` — reconstructed twice, was `de2a84c`). Stage 8 (2026-09-18) forward-committed new `openapi.yaml`/`error-catalog.md` surface past `stage-6c-baseline` **without** reconstructing or moving any tag at the time — see its own section above; the Stage 9 pass below is the genuine sixth reconstruction. See "A0 baseline reconstruction", "A1 baseline reconstruction", "A3 baseline reconstruction", "A3 second baseline reconstruction", "Stage 6C freeze", "Shift/FiscalDay backend... fifth baseline reconstruction", "Store setup & fiscal configuration — Stage 8", and "Shift close... sixth baseline reconstruction" below for the full record of each. |
| **Next action** | Owner decision on `auditEventGet`/`journalEntryGet`'s missing 404 codes; the BIR field review (BIR-006) then a snapshot schema version 2 (payments, PTU/accreditation); printing at the POS after checkout; product CSV import/export and barcode lookup; stock transfers/counts (types exist, no contract operation); the deferred pure-historical-browsing endpoints (`shiftGet`/`shiftList`/`fiscalDayGet`/`fiscalDayList`/`fiscalDayCurrentGet`); or `fiscalInstallationGet`'s proper (reconstruction-backed) completion. |
| **Total ADRs** | 12 |
| **Open `BIR-REVIEW-REQUIRED` items** | 4 (unchanged) |
| **Lines of implementation code written** | 41 migrations, 34 Eloquent models, 14 factories, 3 seeders (Stage 5); Money/Quantity, FinancialCalculator, IdempotencyService, GlobalLockOrder, domain exception hierarchy (Stage 6A); InvoiceSeriesAllocator, AllocatedInvoiceNumber, 2 exceptions (Stage 6B) — all frozen. Stage 6C (uncommitted to any baseline): CheckoutService + 3 resolvers + 9 exceptions + 1 migration, 34 tests. Stages 7–14 (forward commits, no baseline tag touched except the sixth reconstruction): the React admin/POS SPA, store-setup, shift-close/fiscal-day-close, 15 reports, catalog, users and inventory modules. Full regression after Stage 18: Unit 123 + Feature 1 + Database 457 = 581 tests. |

---

## Frozen-corpus rule for Stage 6 onward (owner instruction, 2026-09-16)

Stages 1–6B are closed. **Do not modify any frozen document, ADR,
migration, or schema decision belonging to Stages 1–6B** unless Stage 6C
discovers a genuine contradiction that cannot be implemented against the
frozen corpus as written. If that happens, the required procedure is: (1)
document the contradiction, (2) identify the exact affected invariant/
ADR/API/schema decision, (3) propose the smallest amendment, (4) STOP
and wait for explicit owner approval before changing anything. No frozen
baseline (`stage-1-baseline` through `stage-6b-baseline`) is ever to be
silently moved or its content silently changed.

**Amendment governance note (2026-09-17, baseline linearization):** when an
approved amendment changes an ancestor of stages that are already frozen
and tagged, the correct remedy — while the repository remains
local/unpublished — is to rebuild history so the amendment is a genuine
ancestor of the later baselines, **and** so no earlier baseline contains
any content owned by a later stage. Ancestry alone is not sufficient
evidence that a stage boundary is correct: verify both
`git merge-base --is-ancestor stage-N-baseline stage-(N+1)-baseline`
(ordering) and that `git ls-tree -r --name-only stage-N-baseline` contains
nothing from a later stage's path-ownership (isolation). Stage 6C may not
silently modify: InvoiceSeries ownership by FiscalInstallation, counter
bootstrap semantics, range constraints, series resolution, row-lock
allocation, or rollback behavior (Stage 6B); nor Money/FinancialCalculator,
idempotency, or global lock ordering (Stage 6A).

**Addendum (2026-09-18): Stage 6C is now also frozen.** `stage-6c-baseline` was tagged after Module A's A6 completed `POST /sales`'s HTTP layer and all nine of §16's unblock criteria were met (see "Git baseline history" below). The same discipline now extends one stage further: Stage 7 (and any future Stage 6C forward-fix) may not silently modify `CheckoutService`, its resolvers, or their exceptions without the identical document-contradiction-propose-STOP procedure.

---

## Stage tracker

Stages follow the governing brief §18. Status legend: ✅ Approved & frozen
(tagged) · 🚧 In progress · ⬜ Not started.

| # | Stage | Status | Baseline | Key deliverables | Notes |
|---|---|---|---|---|---|
| 0 | Repository Inspection | ✅ | — | Confirmed empty repo, nothing to preserve | Instant — no prior code existed |
| 1 | Product Baseline | ✅ | `stage-1-baseline` @ `d93a813` | product-vision.md, scope.md, personas.md, market-comparison.md, bir-reference-register.md, sitemap.md | **Reconstructed during the full linearization** to include BIR-012/013/014 (added later, during Stage 2's discount-allocation pass, but Stage-1-owned content) — the original `ac907e1` tag was a point-in-time snapshot, not Stage 1's final approved state. Content-equivalence verified via empty diff against the previously accepted tip. |
| 2 | Domain Model | ✅ | `stage-2-baseline` @ `5a7382a` (substantive `6289588`) | domain-model.md, invariants.md (81 invariants), state-machines.md, erd.md | Final content includes every approved amendment: the original domain model, Void/Refund processing context, capability-catalog sync, Void wording fix, NON_VAT tax-summary model, and InvoiceSeries counter-bootstrap/FiscalInstallation-binding. Now a genuine ancestor of Stage 3, containing nothing from Stage 3 onward — verified by direct tree inspection, not assumed. |
| 3 | Architecture | ✅ | `stage-3-baseline` @ `78a190c` (substantive `680ac1b`) | architecture.md, deployment.md, offline-strategy.md, erd.md (Stage 2 content, retained here), 12 ADRs | **Semantically unchanged** — content-equivalence to the original `c8a8bbe` verified via empty diff. Hash changed only because it now correctly descends from the amended Stage 2, instead of Stage 2 being amended on top of it. No new Stage 3 design decision introduced. |
| 4 | API Contract | ✅ | `stage-4-baseline` @ `2313a16` (substantive `9adc30b`) | openapi.yaml (86 ops, 67 schemas), api-design.md, error-catalog.md, operation-inventory.md, csv-export-contract.md, 13 example files | **Semantically unchanged** — content-equivalence to the previously approved, NON_VAT-amended `515e2db` verified via empty diff. InvoiceSeries's amendment never touched Stage 4 at all. |
| 5 | Database Schema | ✅ | `stage-5-baseline` @ `4c16ee0` (substantive `9de98ce`) | 41 migrations, 34 models, database-schema.md, constraint-register.md, context-integrity-matrix.md, index-strategy.md, migration-plan.md, schema-validation.md, stage-5-report.md, 14 factories, 3 seeders, Stage 5's 9 test files | Content-equivalence to the previously accepted reconciled `f036da9` verified via empty diff. Migration round-trip (`fresh`/`reset`/`migrate`) re-verified clean at this exact boundary before advancing. |
| 6A | Backend — Transaction Foundation | ✅ | `stage-6a-baseline` @ `47fa84d` (substantive `823c032`) | Money/Quantity, FinancialCalculator, CanonicalRequestHasher/IdempotencyService, GlobalLockOrder, domain exception hierarchy | Content-equivalence to `570dd75` verified via empty diff. Full regression re-verified clean at this boundary before advancing. |
| 6B | Backend — Invoice Series Allocation | ✅ | `stage-6b-baseline` @ `a667edc` (substantive `2f5e6e8`; boundary-owned doc fix `a64111e`) | InvoiceSeriesAllocator (row-lock allocation per ADR-004), AllocatedInvoiceNumber DTO, InvoiceSeriesExhaustedException/InvoiceSeriesResolutionException | Content-equivalence to the previously accepted `228e033` verified via empty diff. |
| 6C | Backend — Sale Finalization | ✅ | `stage-6c-baseline` @ `de2a84c` | `CheckoutService` (ADR-003 orchestration), `FiscalInstallationResolver`/`InventoryLocationResolver`/`TaxRegistrationResolver`, 9 domain exceptions, `inventory_locations` default-uniqueness migration, plus (A6) `SaleController`/`SaleFinalizeRequest`/`SaleDetailResource` and friends | **Checkout domain/service layer VERIFIED** (34 tests) — see `docs/06-backend/stage-6c-sale-finalization.md`. **HTTP layer built (A6)**: `POST /sales` resolves `terminal_id`/`cashier_id` exclusively from Module A's `PosRequestContext`, proven never readable from the request body. All nine of `module-a-auth-terminal-initialization.md` §16's unblock criteria met. **FROZEN on explicit owner instruction (2026-09-18).** |
| 7 | Frontend | 🚧 | — | React SPA (React Router, same-origin, no Inertia) on a dark back-office shell (sidebar / icon rail / drawer): login, POS checkout, terminal enrollment, store setup (Stage 8), reports (Stage 11), catalog (Stage 12), users (Stage 13), inventory (Stage 14). See `docs/06-ui/` and the per-stage sections above | Built screen by screen as each backend module landed, every one verified end-to-end in a real browser against the real backend. Not built: audit log and electronic journal screens (no backend routes), product CSV import/export, sales history, void/refund screens. |
| 8 | Testing | ⬜ | — | Unit/feature/invariant test suite | invariants.md's 81 invariants are the test-case source list once this starts |
| 9 | Deployment | 🚧 | — | Docker Compose, backup/restore, deployment checklist | deployment.md was already produced ahead of schedule as part of Stage 3 |

---

## Module tracker (governing brief §3, Modules A–L)

Legend: ✅ Covered · 🚧 Partial/implicit · ⬜ Not started.

| Module | Domain (St.2) | Architecture (St.3) | API Contract (St.4) | DB (St.5) | Backend (St.6) | Frontend (St.7) | Tests (St.8) |
|---|---|---|---|---|---|---|---|
| A — Authentication & Users | ✅ | ✅ | ✅ | ✅ | 🚧 (A0–A6 complete: login/logout/me, RBAC, terminal enrollment, authoritative request-context composition, a fully-audited HTTP test matrix, and Stage 6C's checkout wiring; user management built in Stage 13: `userList`/`userCreate`/`userGet`/`userUpdate`/`userDeactivate` plus the forward-committed `userActivate`) | 🚧 (login, `/admin/users`) | ⬜ |
| B — Store Settings | ✅ | ✅ | ✅ | ✅ | ✅ (Stage 8: tax registrations; Stage 18: `storeSettingsGet`/`storeSettingsUpdate`) | ✅ (`/admin/store-setup/business`, `/admin/store-setup/tax-registrations`) | ⬜ |
| C — Product Catalog | ✅ | ✅ | ✅ | ✅ | 🚧 (Stage 12: `productList`/`Get`/`Create`/`Update`/`Activate`/`Deactivate`, `categoryList`/`Create`, `brandList`/`Create`; `productImport`/`productExport`/`productLookupByBarcode` still ⬜) | 🚧 (`/admin/catalog/*`) | ⬜ |
| D — POS Cashier Screen | ✅ | ✅ | ✅ | ✅ | ⬜ | 🚧 (Stage 7 pass 2: `/pos` checkout screen and terminal enrollment) | ⬜ |
| E — Payment | ✅ | ✅ | ✅ | ✅ | 🚧 (payment sufficiency/recording implemented inside `CheckoutService`, not a standalone module yet) | ⬜ | ⬜ |
| F — Sales Transaction | ✅ | ✅ | ✅ | ✅ | ✅ (`POST /sales`, `saleList`/`saleGet`; Stage 17 adds `invoiceGet`/`invoiceReprint`) | 🚧 (`/pos`, `/admin/sales`) | ⬜ |
| G — Invoice | ✅ | ✅ | ✅ | ✅ | ✅ (Stage 6B allocator frozen; checkout writes the invoice and its snapshot; Stage 17: `invoiceGet`/`invoiceReprint`, versioned snapshot renderer behind the `InvoicePrinter` seam) | 🚧 (Invoice panel on the sale page; no POS-side print yet) | ⬜ |
| H — Cashier Shift | ✅ | ✅ | ✅ | ✅ | 🚧 (`shiftOpen`/`shiftCurrentGet` now real and HTTP-reachable, atomically resolving-or-opening the terminal's `FiscalDay`; `CheckoutService` resolves/locks the terminal's open shift+fiscal_day; Stage 9 added `shiftClose`, cash movements, X-Readings and `fiscalDayClose`/Z-Reading; `shiftGet`/`shiftList`/`fiscalDayGet`/`fiscalDayList`/`fiscalDayCurrentGet` still ⬜) | ⬜ | ⬜ |
| I — Inventory | ✅ | ✅ | ✅ | ✅ | ✅ (Stage 14: all five operations; `StockLedger` is the sole writer of `stock_movements`/`stock_balances`, used by checkout and the stock operations; transfers and stock counts have no contract operation) | 🚧 (`/admin/inventory/stock`, `/admin/inventory/movements`) | ⬜ |
| J — Sales History | ✅ | 🚧 | ✅ | ✅ | ✅ (Stage 15: `saleList`/`saleGet`) | ✅ (`/admin/sales`, `/admin/sales/:saleId`) | ⬜ |
| K — Void and Refund | ✅ | ✅ | ✅ | ✅ | ✅ (Stage 15: request, approve, reject and list/get for both, dispositions, settlements, stock reversal) | ✅ (refund and void panels, `/admin/sales/approvals`) | ⬜ |
| L — Reports | 🚧 | ✅ | ✅ | ✅ | ✅ (Stage 10: all 15 operations, JSON + CSV) | ✅ (Stage 11: `/admin/reports`, one schema-driven viewer) | ⬜ |

---

## Documentation inventory

Unchanged from before the linearization — see the path-ownership map above,
which is definitional: every file listed for a stage there is exactly what
that stage's baseline commit contains, verified by direct tree inspection
rather than by convention alone.

---

## Git baseline history

| Commit | What it represents |
|---|---|
| `ac907e1` | Original Stage 1 tag — later found not to be Stage 1's final content (see below) |
| `d93a813` — **`stage-1-baseline`** | **Reconstructed Stage 1.** Adds BIR-012/013/014 to `bir-reference-register.md`, folding in a Stage-1-owned amendment that had been committed out of order during Stage 2's discount-allocation pass. Content-equivalence to the final approved tip verified via empty diff. |
| `6289588` | Stage 2 substantive content (final, all amendments included) |
| `5a7382a` — **`stage-2-baseline`** | Stage 2 manifest boundary. Genuine ancestor of Stage 3; contains nothing from Stage 3 onward. |
| `680ac1b` | Stage 3 substantive content — content-identical to the original `c8a8bbe` |
| `78a190c` — **`stage-3-baseline`** | Stage 3 manifest boundary. |
| `9adc30b` | Stage 4 substantive content — content-identical to the previously approved `515e2db` |
| `2313a16` — **`stage-4-baseline`** | Stage 4 manifest boundary. |
| `9de98ce` | Stage 5 substantive content — content-identical to the previously accepted `f036da9` |
| `4c16ee0` — **`stage-5-baseline`** | Stage 5 manifest boundary. Migration round-trip re-verified clean here. |
| `823c032` | Stage 6A substantive content — content-identical to the previously accepted `570dd75`'s substantive commit |
| `47fa84d` — **`stage-6a-baseline`** | Stage 6A manifest boundary. Full regression re-verified clean here. |
| `2f5e6e8` | Stage 6B substantive content (allocator) — content-identical to the previously accepted `228e033` |
| `a64111e` | Stage 6B substantive content (doc correction) — fixes `stage-6b-invoice-series-allocation.md`'s stale "not yet retagged" claim; folded into the boundary because that file is Stage 6B's own artifact, not stage-neutral |
| `a667edc` — **`stage-6b-baseline`** | Stage 6B manifest boundary — closes the full linearization. **This is the sealed boundary; it is not `main`'s tip.** |
| `3fb7c70`-equivalent → split into `a64111e`/`a667edc` above, plus the stage-neutral governance commit that follows | The original single "governance-hardening" commit mixed Stage-6B-owned content (the doc fix) with stage-neutral content (validator script, Stage Baseline Rule). Split after the owner identified the ambiguity; see "Where the Stage 6B boundary actually is" above. |
| `8af1ca8` | **Stage-neutral governance** — `scripts/validate-baselines.sh`, the Stage Baseline Rule, the commit convention, and the manifest section recording them. Sits on `main` after `stage-6b-baseline`, deliberately untagged as any stage's boundary. Stage 6C branches from here, not from the tag. |
| `a0736b7` | **Stage 6C initialization** — scope, evidence, operation inventory, proposed file-ownership map, and every gap requiring an owner ruling, written up in `docs/06-backend/stage-6c-sale-finalization.md` before any implementation code. |
| `fb757b2` | **Stage 6C substantive implementation, round one** — `CheckoutService`, `FiscalInstallationResolver`/`InventoryLocationResolver`, 8 new exceptions, the default-location migration, 20 tests, and forward corrections to `Sale`/`InvoiceSeries`/`ElectronicJournalEntry` (round-one fix). |
| `6803871` | **Stage 6C verification pass** — closed four owner-required gates: `TaxRegistrationResolver` (replacing an inline `.first()` query), an adversarial atomicity test, a `CheckoutService`-level concurrency test, and full semantic (not row-count-only) verification of the happy-path test. Also switched `ElectronicJournalEntry` to plain `HasUuids` (cleaner than round one's override). |
| `d2e8244` | **`transaction_number` ruling finalized** — `T-` + ULID, confirmed distinct from `Sale.id` (UUIDv7) and `Invoice.invoice_number`, closed with three dedicated tests (shape, 10-worker concurrency, idempotent-replay consistency). No open items remain in any of Stage 6C's four verification gates. |
| `050ee2f`, `a48cf72`, `f66ed06`, `af1615f`, `2342e6a`, `e4e3cc3`, `73587d1` | **Module A initialization/evidence pass** — scope/evidence document, the `CheckoutService` cashier/shift authorization forward-fix, the closed Decision Register, and its wording corrections (§14, see `module-a-auth-terminal-initialization.md`). No Module A implementation code. |
| `d9f8acd` | **A6 substantive implementation** — `SaleController`/`SaleFinalizeRequest`/`SaleDetailResource` and friends, wiring A4's `PosRequestContext` into `CheckoutService::finalize()`. See `module-a-auth-terminal-initialization.md` §22. |
| `de2a84c` — **`stage-6c-baseline`** | **Stage 6C manifest boundary.** Extends `scripts/validate-baselines.sh` with a new `OWNERSHIP_stage_6c_baseline` pattern, updates `stage-6c-sale-finalization.md`'s disposition and this manifest to record the freeze. A forward tag on `main`'s existing history — no reconstruction, no backup tag; Stages 1–6B entirely untouched. Full regression (`tests/Unit` 102/474, `tests/Database` 208/736, `tests/Feature` 1/1, Pint) green before tagging; `scripts/validate-baselines.sh` 14/14 (7 ordering + 7 isolation checks) after. |

**A0 baseline reconstruction (2026-09-17)** — applying §14's two approved amendments required rebuilding every downstream baseline (see "A0 baseline reconstruction" above for the full record; summarized here for the git history):

| Commit | What it represents |
|---|---|
| `d8f05f9` — **new `stage-4-baseline`** (was `2313a16`) | Stage 4 amendment: `terminalCookieAuth`, conjunctive POS_TERMINAL security requirements, `RATE_LIMITED`. Built on the *unchanged* `stage-3-baseline`. |
| `955c6df`, `a5ba36f` | Stage 5 substantive content replayed (content-identical to the original `9de98ce`/`4c16ee0`) |
| `d5fdf87` — **new `stage-5-baseline`** (was `4c16ee0`) | Adds the `terminals.credential_hash` partial unique index migration + `TerminalCredentialHashUpgradeTest` (8 tests), completing Stage 5's own declared ADR-011 intent. |
| `1c8074b`, `6250574` — **new `stage-6a-baseline`** (was `47fa84d`) | Stage 6A replayed, content-identical to the original `823c032`/`47fa84d`. |
| `a52391f`, `b71a5ea`, `f2ddef0`, `d2b2f03`, `f4601ca` — **new `stage-6b-baseline`** (was `a667edc`) | Stage 6B, the validator/governance commit, an ownership-map sync for the new Stage 5 test file, and the manifest freeze replayed, content-identical except the sync fix. |
| `32166e4` … `54a7e1a` | Every post-`stage-6b-baseline` commit that existed on `main` before A0 (Stage 6C implementation through the Module A wording corrections), replayed unchanged onto the reconstructed chain. `54a7e1a` is the new `main` tip. |

**A1 baseline reconstruction (2026-09-18)** — a second reconstruction, needed because A1's sealing correction had added `VALIDATION_FAILED` to `error-catalog.md` as a forward commit rather than folding it into `stage-4-baseline` (see "A1 baseline reconstruction" above for the full record):

| Commit | What it represents |
|---|---|
| `f57e924` — **new `stage-4-baseline`** (was `d8f05f9`) | Adds `VALIDATION_FAILED` (422) to `error-catalog.md`. Built on the *unchanged* `stage-3-baseline`. |
| `955c6df`, `a5ba36f`, `678d597` — **new `stage-5-baseline`** (was `d5fdf87`) | Stage 5 (incl. the `credential_hash` index) replayed, content-identical to A0's own output. |
| `1c8074b`, `1c0e5c9` — **new `stage-6a-baseline`** (was `6250574`) | Stage 6A replayed, content-identical. |
| `a52391f`, `b71a5ea`, `d1ac152`, `1df2587`, `d50abd7` — **new `stage-6b-baseline`** (was `f4601ca`) | Stage 6B, the validator/governance commit, and the Stage-5-ownership sync replayed, content-identical. |
| `32166e4` … `ec95b4b` | Every post-`stage-6b-baseline` commit that existed on `main` before this reconstruction (Stage 6C, Module A docs, the A0 correction, and the full A1 implementation including its own sealing correction), replayed onto the reconstructed chain. Cherry-picking the original `VALIDATION_FAILED` forward-fix (`d38d6e4`) onto the amended Stage 4 boundary produced a clean 3-way-merge no-op for `error-catalog.md` alone (the line was already present) while its other two files applied normally. `ec95b4b` is the new `main` tip — content-identical (`git diff` empty) to the preserved pre-reconstruction tip. |

**Superseded (all previous reconciliation attempts, reachable via `git reflog`/`backup/pre-final-baseline-reconcile`, `backup/pre-module-a-a0`, or `backup/pre-a1-validation-failed-reconstruction`, no longer referenced by any tag):** the original interleaved history (`6da07bd`, `b7ef8fd`, `c8a8bbe` (as a Stage-2-ancestor-containing tag target), `40aaebd`/`515e2db`, `21bf0c2`/`8c8fe32`, `47baddf`/`48d173e`), the rejected first InvoiceSeries reconciliation (`d1bfaa7`, `f4ca1d9`), the rejected two-part reconciliation (`e1fb82a`, `f036da9`, `570dd75`, `228e033`, `ceab7d4`), `b335a7a` (the first `stage-6b-baseline` candidate) and `3fb7c70` (the original ungrouped governance commit); the pre-A0 `stage-4-baseline`/`stage-5-baseline`/`stage-6a-baseline`/`stage-6b-baseline` targets (`2313a16`, `4c16ee0`, `47fa84d`, `a667edc`) and the pre-A0 `main` tip (`73587d1`), reachable via `backup/pre-module-a-a0`; and A0's own output, now superseded by the A1 reconstruction — `d8f05f9`, `d5fdf87`, `6250574`, `f4601ca`, and the pre-A1-reconstruction `main` tip `d25e802`, reachable via `backup/pre-a1-validation-failed-reconstruction`. `c8a8bbe` and `515e2db` remain historically meaningful as "the commit whose *content* Stage 3/4 were verified against," but are no longer any tag's target.

**Verification performed before any tag was moved:**
1. **Ancestry** — `git merge-base --is-ancestor` for every adjacent pair (`stage-1-baseline`→`stage-2-baseline`, …, `stage-6a-baseline`→`stage-6b-baseline`): all six return true.
2. **Isolation** — `git ls-tree -r --name-only <stage-N-baseline>` grepped for every later stage's path-ownership at each of the five internal boundaries: all empty.
3. **Content-completeness** — `git diff backup/pre-final-baseline-reconcile <final tip> -- . ':!docs/PROJECT-MANIFEST.md'` is empty: every file that existed in the previously accepted (content-correct) state still exists, nothing was duplicated, nothing was dropped.
4. **Behavioral regression** — full migration round-trip, full Unit/Database/Feature suites, and every named critical test, all re-run at the final tip and matching the pre-linearization counts exactly.

**Verification performed before A0's tags were moved:**
1. **Ancestry** — `git merge-base --is-ancestor` for every adjacent pair, including `stage-3-baseline`→(new)`stage-4-baseline`, run against the candidate commits before any tag moved, then re-run against the moved tags afterward: all seven (including `stage-6b-baseline`→`main`) return true both times.
2. **Isolation** — `scripts/validate-baselines.sh`: 12/12 (6 ordering + 6 isolation; Stage 6B is N/A as the last stage) both on the pre-A0 tags and on the post-A0 tags.
3. **Boundary isolation, per-stage** — at each of the four reconstructed candidate boundaries, confirmed by direct `git ls-tree` inspection that the amendment/content due at that boundary was present and nothing from the next stage existed yet (e.g. the new `stage-4-baseline` has `terminalCookieAuth` but no `credential_hash` migration or `app/Domain/Financial/`; the new `stage-5-baseline` has the migration but no Stage 6A code; etc.).
4. **Content-completeness** — `git diff backup/pre-module-a-a0 <reconstructed main tip>` touches exactly 6 files: the Stage 4 contract amendment (2 files), the Stage 5 credential_hash migration + its test (2 files), and two ownership-map sync lines (`scripts/validate-baselines.sh`, this manifest). Nothing else in the tree differs.
5. **Behavioral regression** — full migration round-trip (`fresh`, `reset`+`migrate`, amendment rollback/reapply), `tests/Unit` (85/196), `tests/Database` (139/489, i.e. the pre-A0 131/476 plus the 8 new credential_hash tests), `tests/Feature` (1/1), the Stage 6A/6B/6C concurrency suites individually, and Pint — all green on the reconstructed `main`.

**Verification performed before the A1 reconstruction's tags were moved:**
1. **Ancestry** — `git merge-base --is-ancestor` for every adjacent pair (`stage-3-baseline`→new `stage-4-baseline`, …, new `stage-6b-baseline`→`main`), run against the candidate commits, then re-run against the moved tags afterward: all five return true both times.
2. **Isolation** — `scripts/validate-baselines.sh`: 12/12, both on the pre-reconstruction tags and on the post-reconstruction tags.
3. **Boundary isolation, per-stage** — direct `git ls-tree` inspection at each of the four new candidate boundaries: `VALIDATION_FAILED` present at the new `stage-4-baseline` with no Stage 5+ content yet; the `credential_hash` migration present (only) at the new `stage-5-baseline`; Stage 6A/6B code appearing at their own boundaries and not before; A1's own code (`AuthController.php`) absent from the new `stage-6b-baseline`.
4. **Content-completeness** — `git diff backup/pre-a1-validation-failed-reconstruction <reconstructed main tip>` is **empty** — stronger than A0's "exactly N files": here the reconstruction only relocated where `VALIDATION_FAILED` was introduced in history, never changed any file's final content.
5. **Behavioral regression** — full migration round-trip, `tests/Unit` (89/209), `tests/Database` (160/566), `tests/Feature` (1/1), the Stage 6A/6B/6C concurrency suites individually, and Pint — all green on the reconstructed `main`, matching the pre-reconstruction counts exactly.

---

## Open items

### `BIR-REVIEW-REQUIRED` (4 remaining — see [bir-reference-register.md](01-research/bir-reference-register.md))
1. **BIR-006** — full required invoice-field list needs re-confirmation against RR 16-2018/6-2022/11-2004 directly.
2. **BIR-007** — PTU status for CRM/POS should be re-confirmed against current RMO 24-2023 text before any real filing.
3. **BIR-008** — no fixed EIS activation date for standalone POS users yet.
4. **BIR-010** — exact date ORUS gained CAS/POS registration functionality is unverified.

### Standing risks to watch
- **The repository is published** (`origin` = `https://github.com/iamrgalisanao/tindaflow.git`, `main` and the eight `stage-*-baseline` tags pushed; `backup/*` tags are local only). Any future baseline reconstruction now means force-pushing `main` and the moved tags, which is why Stage 13 forward-committed `USER_NOT_FOUND` as a recorded one-time exception. The next code that fills a gap in an already-frozen response reopens that question.
- **Two known gaps carried from Stage 13**: the API can still deactivate the last active ADMIN (only the screen guards it), and `AdminUserSeeder` will not recreate an admin while any ADMIN row exists, even an inactive one.
- The 14-operation idempotency contract needs consistent Stage 6C implementation, not just per-operation ad hoc handling.
- Void-eligibility policy and the FiscalDay auto-open policy are TindaFlow product decisions, not BIR mandates.
- **Disclosed Stage 6C contract gaps**: the frozen Stage 4 error catalog has no code for "no default inventory location configured," "no/ambiguous active tax registration," or the terminal/fiscal-installation setup-defect cases — `InventoryLocationResolutionException`/`TaxRegistrationResolutionException`/`FiscalInstallationResolutionException` currently surface as unhandled `RuntimeException`s rather than stable HTTP error envelopes. Deliberate (no new code invented unilaterally per the Stage 6C rulings) but a real gap a future Stage 4 amendment or the eventual HTTP layer must close before production use. See `docs/06-backend/stage-6c-sale-finalization.md` §5.
- **`POST /sales` cannot go to production until Module A exists**: `CheckoutService` accepts `$terminalId`/`$cashierId` as plain parameters by design; the future controller must resolve these from an authenticated/authorized terminal context, never from caller-controlled request fields. This is documented as a binding constraint on that not-yet-built controller, not merely a suggestion.

---

## Revision log

- **2026-09-16/17** — Stages 1–6B built up through many remediation and amendment passes on a single interleaved branch (full detail preserved in git history and in `docs/06-backend/stage-6b-invoice-series-allocation.md` §8–9). Two internal reconciliation attempts (NON_VAT amendment placement, then a two-part InvoiceSeries reconciliation) each corrected a real governance defect but the second was itself found to be structurally insufficient.
- **2026-09-17** — **Owner rejected the two-part reconciliation** (`e1fb82a`/`f036da9`/`570dd75`/`ceab7d4`): although its resulting file content was correct, `stage-2-baseline` (`e1fb82a`) still had Stage 3/4/5's original commits as git ancestors, because the reconstruction started from the old Stage 5 tip rather than the true Stage 2 boundary. The owner further identified that this pattern was not new — the project's original `stage-2-baseline` had always postdated Stage 3 in the commit graph — and required a full linearization rather than a second narrow patch, with an explicit backup (`backup/pre-final-baseline-reconcile` @ `ceab7d4`) before any further rewriting.
- **2026-09-17** — **Full baseline linearization performed and validated.** Built each stage boundary by checking out that stage's exact final-approved content from the backup tip onto the previous boundary (Stage 1 → 2 → 3 → 4 → 5 → 6A → 6B), verifying content-equivalence via empty diffs at every step and re-running the relevant test suite before advancing. **Found and fixed one genuine gap along the way**: Stage 1's own `ac907e1` tag was not Stage 1's final content (missing the later BIR-012/013/014 amendment) — fixed with a dedicated commit and the whole chain rebased onto it (zero conflicts). **Verification**: all six ancestry checks pass; all five internal isolation checks are empty; the final tip's full tree is diff-identical to the pre-linearization backup outside this manifest; migration round-trip, full Unit (85/196)/Database (98/305)/Feature (1/1) suites, every named critical test, and Pint all re-verified clean and matching pre-linearization counts exactly. `main` fast-forwarded to the final tip (`b335a7a`); `stage-1-baseline` through `stage-6b-baseline` all moved to their reconstructed hashes; scratch branches deleted.
- **2026-09-17** — **Owner withheld final sign-off pending independent re-verification** (the operation moved every baseline tag) and raised the key question precisely: were BIR-012/013/014 *formally determined* to be Stage 1 amendments, or merely content that happened to land there? Independent pass performed (ancestry, tag resolution, isolation extended to Stage 1, content-equivalence for 3/4/5/6A/6B, a full suite run from a separate `git worktree` checkout of the tag itself, git-state cleanliness, a stale-reference sweep) — full results in "Independent verification pass" above. The Stage 1 question was answered from contemporaneous evidence (the introducing commit's own disclaimer plus the first manifest's own contemporaneous classification), not inference. Added `scripts/validate-baselines.sh` and formalized the Stage Baseline Rule and the two-commit-per-stage convention, initially as one follow-up commit (`3fb7c70`) on top of `stage-6b-baseline` (`b335a7a`).
- **2026-09-17** — **Owner identified that `3fb7c70` mixed Stage-6B-owned content with stage-neutral governance content**, and that committing them together made "where the Stage 6B boundary actually is" ambiguous. Resolved by splitting: the correction to `docs/06-backend/stage-6b-invoice-series-allocation.md` (Stage 6B's own artifact per the path-ownership map) was pulled into a new Stage 6B substantive commit (`a64111e`), and `stage-6b-baseline` was moved to a new manifest commit (`a667edc`) built on top of it — superseding `b335a7a`. `scripts/validate-baselines.sh` and the Stage Baseline Rule/verification narrative, which are not owned by any single stage, remain in a stage-neutral commit (`8af1ca8`) that sits after `stage-6b-baseline` on `main`, explicitly documented as such so a future contributor branches Stage 6C from `main` (which carries the governance forward) rather than from the tag alone. Re-verified after the split: `git merge-base --is-ancestor stage-6a-baseline stage-6b-baseline` still true; `scripts/validate-baselines.sh` passes all twelve checks. **Stages 1 through 6B are now closed on a chain that is correctly ordered, correctly isolated, and has its governance boundary explicitly documented rather than left implicit.**
- **2026-09-17** — **Owner authorized Stage 6C (Sale Finalization) to begin, with an explicit first step: scope/evidence initialization before any implementation.** `docs/06-backend/stage-6c-sale-finalization.md` was written (commit `a0736b7`) covering the operation inventory (`POST /sales` only), every domain invariant Sale Finalization must enforce, the exact already-approved APIs it must call (ADR-003's transaction script, `FinancialCalculator`, `IdempotencyService`, `GlobalLockOrder`, `InvoiceSeriesAllocator`), a proposed file-ownership map, and eight flagged gaps requiring an owner ruling — including two real defects discovered in already-frozen Stage 5/6A model files (`Sale`/`InvoiceSeries` missing `$fillable` entries).
- **2026-09-17** — **Owner ruled on every flagged gap** (fiscal-installation resolution uses `[effective_from, effective_to)` semantics with exactly-one-match required; V1 checkout always deducts from the store's single default inventory location, hardened by a new partial-unique migration; the dual idempotency mechanism — `IdempotencyService` + `sales.UNIQUE(terminal_id, idempotency_key)` — is intentional defense-in-depth, not redundant; the two model defects are authorized smallest-forward-corrections, no Stage 5/6A baseline retagged; Module A/authentication is explicitly NOT Stage 6C's scope). `CheckoutService` (commit `fb757b2`) was implemented against these rulings: full ADR-003 orchestration, `FiscalInstallationResolver`/`InventoryLocationResolver`, 8 new domain exceptions, the default-location migration, and 20 tests. A further pre-existing defect was found during integration (not by inference): `ElectronicJournalEntry` used `HasUlids` against a native `uuid` column, generating Base32 strings PostgreSQL rejects — fixed as a Stage 6C forward correction.
- **2026-09-17** — **Owner withheld "complete" for the checkout service/domain layer pending four verification gates**, having identified real gaps in round one: `transaction_number`'s generation algorithm was never actually settled (only its per-store uniqueness scope was frozen, by Stage 5); the tax-registration resolution reused a naive `.first()` query instead of the same zero/one/many rigor already applied to fiscal-installation resolution; no adversarial test proved the two-layer idempotency guarantee end to end through `CheckoutService`; and the happy-path acceptance test verified row counts, not field-level semantics. All four closed (commit `6803871`): a dedicated `TaxRegistrationResolver` (with inclusive-end interval semantics determined from the table's own CHECK constraint, not copied by analogy); a sequential adversarial test forcing failure after business rows are staged, proving full rollback including the idempotency reservation itself, then a same-key retry succeeding exactly once; a new `checkout_worker.php` + `CheckoutServiceConcurrencyTest` proving the two-layer guarantee through real multi-process `CheckoutService` calls; and the happy-path test expanded to 59 field-level assertions across every written table. `ElectronicJournalEntry` was also switched from the round-one `HasUlids`-plus-override fix to plain `HasUuids` (Laravel's own UUIDv7 generator is already time-sortable, needing no custom code) per the owner's suggestion.
- **2026-09-17** — **Owner confirmed the `transaction_number` ruling** (commit `d2e8244`): `T-` + a 26-character ULID, a deliberately separate operational identifier from `Sale.id` (UUIDv7 via `HasUuids`) and `Invoice.invoice_number` (the BIR accountable-document serial, `InvoiceSeries`-allocated) — never derived from either, generated server-side only inside the authoritative checkout/idempotency execution path, never manufactured again on replay. Closed with three required tests: shape/regex validation, a 10-worker high-volume concurrency stress (matching `InvoiceSeriesAllocatorConcurrencyTest`'s established pattern), and an idempotent-replay assertion. **This closes all four verification gates with no open items.** Current disposition: **checkout domain/service layer VERIFIED; `POST /sales` production readiness remains BLOCKED ON MODULE A, not on any of these four gates.** Full regression: `tests/Unit` 85/196, `tests/Database` 130/466, `tests/Feature` 1/1, migration round-trip clean, Pint clean, `scripts/validate-baselines.sh` all 12 checks pass. **No `stage-6c-baseline` tag exists yet — Stage 6C is not frozen, and per the owner's standing instruction, Stage 6D does not begin until it is.**
- **2026-09-17** — **Owner authorized a Module A (Authentication/Authorization/Terminal enrollment) initialization/evidence pass, explicitly scoped to research only** — no controllers, middleware, policies, or auth code. Produced `docs/06-backend/module-a-auth-terminal-initialization.md` (17 sections: frozen-source inventory, operation inventory, ADR-011 trace, auth/enrollment contracts, authoritative context model, RBAC mapping, endpoint classification, persistence ownership, error-catalog mapping, test matrix, and a Decision Register). **One real Stage 6C defect found and fixed during evidence-gathering** (not deferred, per explicit owner instruction): `CheckoutService`'s shift resolution checked `terminal_id` only, never `cashier_id`, so once real authentication existed, one cashier could attach a checkout to a different cashier's still-open shift on a shared terminal — fixed in `a48cf72` with a full negative-write-set regression test. Across several rounds of owner critique (each verified against actual schema/code, not assumption), the Decision Register closed all nine open items, including two requiring changes to already-frozen content: the Stage 4 OpenAPI contract (`terminalCookieAuth`, conjunctive POS_TERMINAL security, `RATE_LIMITED`) and a Stage 5 schema completion (`terminals.credential_hash` partial unique index) — both correctly identified as requiring full baseline reconstruction rather than a forward commit, batched into a single step (**A0**) for the next session.
- **2026-09-17** — **A0 baseline reconstruction performed and validated**, applying both amendments from the closed Decision Register. See "A0 baseline reconstruction" above for the full record: `main` preserved as `backup/pre-module-a-a0` (`73587d1`); every stage from the current `stage-4-baseline` forward rebuilt on a scratch branch by cherry-picking each stage's actual substantive commits (from `git log`, not assumed); Stage 4 amendment applied and validated (OpenAPI parses, all 13 POS_TERMINAL operations conjunctively require both credentials); Stage 5's `credential_hash` partial unique index added with 8 dedicated tests (multiple NULLs, single/distinct/duplicate non-null hashes, rollback/reapply, legacy-row survival, abort-without-mutation on legacy duplicates) and folded into the same Stage 5 replay step; Stage 6A and 6B replayed with their concurrency suites re-verified; all 13 post-`stage-6b-baseline` commits replayed unchanged. Content-equivalence against the preserved backup confirmed via `git diff`: exactly 6 files differ, all intentional. Full regression green (`tests/Unit` 85/196, `tests/Database` 139/489, `tests/Feature` 1/1, Stage 6A/6B/6C concurrency suites, migration round-trip including the amendment's own rollback/reapply, Pint) before any tag moved. `main` and `stage-4-baseline`/`stage-5-baseline`/`stage-6a-baseline`/`stage-6b-baseline` updated to the reconstructed chain; `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `scripts/validate-baselines.sh` 12/12 on the new tags. **A1 (Module A's own authentication/authorization implementation) has not started.**
- **2026-09-17** — **Owner corrected Ruling 6's disproved premise** (commit `add582b`, documentation-only, no baseline touched): A0's own OpenAPI inspection had shown `components.responses.TooManyRequests` was already valid, not dangling as the evidence pass originally suspected — only the `RATE_LIMITED` catalog code was actually missing/applied. Ruling 6 and every other place repeating the "dangling `$ref`"/"define `TooManyRequests`" claim were corrected to match, with the original (mistaken) finding preserved alongside the correction rather than erased. `scripts/validate-baselines.sh` re-run clean (12/12); no tag moved. This closed A0.
- **2026-09-18** — **A1 (human authentication/session foundation) implemented** (commit `c695d23`): `POST /auth/login`, `POST /auth/logout`, `GET /auth/me` on standard Laravel database-backed session auth (no Sanctum/Passport/tokens). Full detail in `docs/06-backend/module-a-auth-terminal-initialization.md` §17: `Auth::attempt()`-based login with identical external behavior for unknown-email/wrong-password/inactive-user, session-fixation protection (regenerate on success), `EnsureUserIsActive` middleware revalidating `active` on every protected request (forced logout, not a per-endpoint-only rejection, when a user goes inactive mid-session), a named `login` rate limiter (email+IP, centralized config, no magic numbers) rendering `429 RATE_LIMITED`, and a `UserSummaryResource`/`RoleCapabilityCatalog` pair centralizing the frozen `UserSummary` shape so login/`me` can't drift. `bootstrap/app.php` now renders `DomainException`/`AuthenticationException`/`TooManyRequestsHttpException` into the frozen error envelope for the first time (no HTTP layer existed before this pass to need it), and a new `AssignRequestId` middleware attaches `request_id` to every response. One known, disclosed gap left deferred, not invented: `LoginRequest`'s own 422 structural-validation failures still use Laravel's default shape, matching §11's pre-existing "structurally-bad login" CONTRACT GAP (not exercised by any required test). 15 new Database tests (53 assertions) plus 4 new Unit tests (13 assertions) — placed under `tests/Database/` rather than `tests/Feature/` because every migration in this project is PostgreSQL-only and cannot run against phpunit.xml's default sqlite connection. Full regression: `tests/Unit` 89/209, `tests/Database` 154/542, `tests/Feature` 1/1, Stage 6A/6B/6C concurrency suites individually re-verified, migration round-trip clean, Pint clean, `scripts/validate-baselines.sh` 12/12. No baseline tag moved; `backup/pre-module-a-a0` retained through this pass. **A2 (RBAC/capability enforcement) has not started.**
- **2026-09-18** — **Owner declined to seal A1**, correctly identifying the disclosed `422` deferral as a genuine A1 contract defect rather than an acceptable gap (`authLogin`'s `422` is part of the frozen contract, and every other `422` in `openapi.yaml` already shares the same generic envelope), and asked for the real pre-authentication CSRF-bootstrap path to be proven rather than assumed. **Corrected** (commit `d38d6e4`): one new stable code, `VALIDATION_FAILED` (422, `error-catalog.md`), rendered by a single centralized `ValidationException` handler in `bootstrap/app.php` (not a per-`FormRequest` override) — a forward addition, not a Stage 4 amendment, since nothing already-frozen needed to retroactively contain it. The CSRF-bootstrap flow was verified end-to-end with a genuinely cookie-less test client and real CSRF enforcement forced on: `routes/web.php`'s existing `GET /` application-shell route (already in the `web` middleware group) sets `XSRF-TOKEN` on every response through it, which a fresh browser presents as `X-XSRF-TOKEN` alongside its new session cookie to successfully `POST /api/v1/auth/login` — no login exemption, no Sanctum, no new endpoint. 6 new tests (5 validation-envelope cases, 1 CSRF-bootstrap flow) bring `AuthenticationSessionTest.php` to 21 methods/77 assertions. Full regression re-verified: `tests/Unit` 89/209, `tests/Database` 160/566, `tests/Feature` 1/1, Stage 6A/6B/6C concurrency suites, migration round-trip, Pint, `scripts/validate-baselines.sh` 12/12 — all clean; no baseline tag moved; `backup/pre-module-a-a0` retained. **A1 is now sealed; A2 is authorized to begin.**
- **2026-09-18** — **Owner identified that the previous entry's "no baseline tag moved" premise was itself the exact class of defect A0 was created to prevent**: `VALIDATION_FAILED` was added to `docs/05-api/error-catalog.md` (Stage-4-owned, frozen content) as an ordinary forward commit, which A0's own report already establishes is not a legitimate way to amend a file `stage-4-baseline` freezes — it leaves the baseline stale again for the identical structural (ancestry-ordering) reason A0 explains. Instructed a governance check first: inspect the complete `error-catalog.md`/`Error`/`UnprocessableEntity` at the actual frozen `stage-4-baseline` (not `main`, which already carried the disputed amendment) for any existing code that could legitimately be reused. **None found** — every code is either protocol-level or tied to a specific business field; repurposing one was explicitly rejected. `VALIDATION_FAILED` was therefore confirmed as a genuine second Stage 4 amendment and folded into a second reconstruction (commit `f57e924` — new `stage-4-baseline`; see "A1 baseline reconstruction" above for the full record). `git diff` against the preserved pre-reconstruction tip came back **completely empty** (not merely small) — the reconstruction only relocated where in history `VALIDATION_FAILED` was introduced, never changed any file's final content. Full regression re-verified unchanged (`tests/Unit` 89/209, `tests/Database` 160/566, `tests/Feature` 1/1, Stage 6A/6B/6C concurrency suites, migration round-trip, Pint, `scripts/validate-baselines.sh` 12/12); `stage-4-baseline`/`stage-5-baseline`/`stage-6a-baseline`/`stage-6b-baseline` moved to their twice-reconstructed hashes; `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; both `backup/pre-module-a-a0` and the new `backup/pre-a1-validation-failed-reconstruction` retained. **A1 is now governance-sealed; A2 is authorized to begin.**
- **2026-09-18** — **A2 (authorization/capability foundation) implemented** (commit `f2f4f84`), scoped narrowly to fixed RBAC only — no terminal enrollment or authoritative context mixed in. Reconnaissance performed first, per instruction: mechanically compared `openapi.yaml`'s `Capability` enum, `domain-model.md` §2.2's catalog, and A1's `RoleCapabilityCatalog` — identical 17-entry sets, no divergence, so no STOP condition applied and no baseline reconstruction was needed. `RoleCapabilityCatalog` gained `CAPABILITIES` (the vocabulary, single source of truth) and `has()`; `ADMIN`'s map entry is now literally `self::CAPABILITIES` rather than a duplicated list. `AppServiceProvider::boot()` registers one `Gate::define()` per capability, each delegating to `RoleCapabilityCatalog::has()` — no ACL engine, no per-resource policies, no database-backed permissions. Whole-operation enforcement uses Laravel's built-in `can:` middleware, chained after `['auth', EnsureUserIsActive::class]` so an inactive user's stale session always gets 401 before any Gate check runs. No production controller yet needs a whole-operation gate (Stage 4's Catalog/Terminal/User endpoints are unimplemented); proved end-to-end via one environment-guarded test-only route instead of building controllers merely to exercise the `x-capability` inventory. `PRICE_OVERRIDE`/`DISCOUNT_OVERRIDE`/`CASH_OUT` remain registered Gate abilities but are confirmed (by a test parsing `openapi.yaml` directly) never applied as a whole-operation `x-capability` or route-level gate anywhere. A real framework subtlety was found and documented: Laravel's `Handler::prepareException()` unconditionally rewraps a status-less `Illuminate\Auth\Access\AuthorizationException` into `Symfony\Component\HttpKernel\Exception\AccessDeniedHttpException` before any custom renderer for the Illuminate type can fire — the `AUTHORIZATION_DENIED` renderer is registered against the Symfony type instead, confirmed against a real denial, not merely reasoned from documentation. 22 new/extended test methods across `tests/Unit/Services/Auth/{RoleCapabilityCatalogTest,CapabilityGateTest,CapabilityOpenApiTraceabilityTest}` and `tests/Database/AuthorizationTest.php`. Full regression: `tests/Unit` 102/474, `tests/Database` 165/578, `tests/Feature` 1/1, the A1 authentication/session suite unchanged (21/77), Stage 6A/6B/6C concurrency suites individually re-verified, Pint clean, `scripts/validate-baselines.sh` 12/12. No baseline tag moved; both backup tags retained. **A3 (terminal enrollment) has not started.**
- **2026-09-18** — **Owner confirmed A2 sealed, subject to one verification**: that the `/api/v1/_test/requires-catalog-manage` route is genuinely absent outside the `testing` environment. Confirmed directly — `php artisan route:list` under both `local` and explicit `APP_ENV=production` shows no `_test` route at all; it appears only under `APP_ENV=testing`. No cleanup needed; A2 sealed as-is.
- **2026-09-18** — **A3 (terminal enrollment/credential verification) implemented** (commit `7e9b9f2`): ADR-011's full enrollment lifecycle (`terminalCreateEnrollmentToken`/`terminalEnroll`/`terminalCurrent`/`terminalList`/`terminalGet`/`terminalRevoke`), one-time SHA-256-hashed enrollment tokens consumed via two deliberately separate transactions (token claim commits before credential issuance is attempted, matching ADR-011's "invalidated immediately... regardless of outcome" literally), and a `tindaflow_terminal` credential cookie fully independent of the human session. Full detail in `docs/06-backend/module-a-auth-terminal-initialization.md` §19: store-scoping, TerminalStatus non-interaction, re-enrollment semantics, and the deferred-not-dropped audit-event decision were all resolved from frozen evidence before writing code, with none requiring a STOP. One decision flagged rather than resolved unilaterally: `terminalEnroll`'s `409` has no error-catalog.md code (`CONCURRENCY_CONFLICT` checked and rejected as a reuse candidate); `ENROLLMENT_TOKEN_INVALID` is implemented as the proposed candidate, not yet catalogued, pending the owner's ruling. **Found and fixed a real, project-wide infrastructure bug**: `config/database.php`'s `pgsql` connection never pinned a session timezone, so PostgreSQL defaulted to the server's own local zone (reproduced directly as `Asia/Kuala_Lumpur`), silently corrupting every timestamptz round-trip by that offset — a genuinely-future `terminal_enrollment_tokens.expires_at` was reading back 8 hours in the past. Fixed with one line pinning the session to UTC, matching `config/app.php`. Proven race-safe under real two-OS-process concurrency (`TerminalEnrollmentConcurrencyTest`, mirroring `IdempotencyConcurrencyTest`'s pattern) and proven that A2's `AUTHORIZATION_DENIED` renderer never swallows `TERMINAL_NOT_ENROLLED`/`TERMINAL_REVOKED`. 23 new test methods (`TerminalEnrollmentTest` 22/65, `TerminalEnrollmentConcurrencyTest` 1). Full regression: `tests/Unit` 102/474, `tests/Database` 188/652, `tests/Feature` 1/1, A1 (21/77) and A2 (22/290) suites unchanged, Stage 6A/6B/6C concurrency suites individually re-verified, Pint clean, `scripts/validate-baselines.sh` 12/12. No baseline tag moved; both backup tags retained. **A4 (authoritative User+Terminal+Store context) has not started.**
- **2026-09-18** — **Owner approved `ENROLLMENT_TOKEN_INVALID` as a genuine third Stage 4 amendment** (based on direct frozen-`stage-4-baseline` inspection rather than force-fitting `CONCURRENCY_CONFLICT`), and required two A3 implementation boundaries verified/fixed before reconstruction so the chain would move only once: (1) `terminalCurrent`'s production route deferred to A4 — a Store-A user holding a Store-B terminal's credential could otherwise observe that terminal before A4's coherence check exists — removed from the production route group and re-registered only under `if (app()->environment('testing'))`, resolver/controller left fully implemented and tested; (2) Store-scoping for terminal management made executable, not just architecturally assumed, with dedicated cross-store tests, plus a `TerminalEnrollmentService::consumeToken()` hardening rejecting a cross-store token claim inside the same locked transaction before consumption. Closed as commit `b98e576` (`fix(backend): A3 closeout -- defer terminalCurrent to A4, prove Store scoping`), also adding: a regression proving a post-claim `issueCredential()` failure never un-consumes the token; a `DatabaseConnectionTimezoneTest` invariant (`SHOW TIME ZONE` = UTC, future timestamps don't shift); and centralizing the terminal-credential cookie's 5-year lifetime into `config('tindaflow.terminal_credential.lifetime_minutes')` rather than a controller magic number. **Process note recorded, not treated as invalidating the implementation**: A3's own STOP conditions listed "new public error code required," and `ENROLLMENT_TOKEN_INVALID` was implemented/forward-committed before that STOP was raised — a workflow deviation to correct for in A4+ (stop the moment a frozen-contract amendment's necessity is recognized, before implementing around it), repaired here by the reconstruction itself.
- **2026-09-18** — **A3 baseline reconstruction performed and validated** (a third reconstruction, same discipline as A0 and the A1 `VALIDATION_FAILED` correction). See "A3 baseline reconstruction" above for the full record: `main` preserved as `backup/pre-a3-enrollment-token-reconstruction` (`b98e576`); `stage-4-baseline` through `stage-6b-baseline` rebuilt on a scratch branch by cherry-picking every stage's actual substantive commits from the current chain; `ENROLLMENT_TOKEN_INVALID` added to `error-catalog.md` at the new `stage-4-baseline` (`776c847`); all post-6B commits (including the A3 implementation and both closeout corrections) replayed cleanly, with the `VALIDATION_FAILED` forward-fix commit (`d00f640`) reconfirmed as an expected no-op re-merge. Content-equivalence against the preserved backup: `git diff --stat` shows exactly **one line** changed (the new catalog row) — not empty, and deliberately so, since (unlike `VALIDATION_FAILED`) `ENROLLMENT_TOKEN_INVALID` was never forward-committed to `main` before this reconstruction began. All 5 ancestry checks and all applicable isolation checks pass (`scripts/validate-baselines.sh`, 12/12, both before and after the tag move). Full regression green at the reconstructed tip before any tag moved: fresh migration, migration round-trip, `tests/Unit` 102/474, `tests/Database` 194/668, `tests/Feature` 1/1, A1/A2/A3 suites individually re-verified, enrollment-token multiprocess concurrency, Stage 6A/6B/6C concurrency suites, OpenAPI YAML parse, Pint. `stage-4-baseline`/`stage-5-baseline`/`stage-6a-baseline`/`stage-6b-baseline` moved to their thrice-reconstructed hashes (`776c847`/`68aafb9`/`26c626e`/`5a24120`); `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `main` force-updated to the reconstructed tip `6884e9f` (a non-fast-forward update, since history was rewritten from `stage-4-baseline` forward — the same pattern used in both prior reconstructions); scratch branch deleted after the move. All four backup tags retained. **A3 is now governance-sealed; A4 is authorized to begin on explicit instruction.**
- **2026-09-18** — **Owner held the previous entry's sealing short**, accepting the third reconstruction and every other closeout item but identifying that the reported Store-scope tests didn't individually prove all four terminal-management operations (`terminalList`, `terminalGet`, `terminalCreateEnrollmentToken`, `terminalRevoke`) are Store-scoped — specifically noting that cross-store token *consumption* and cross-store token *issuance* are different attack paths, and that `terminalGet`/`terminalRevoke` had no dedicated test beyond a shared status-only check. Instructed three explicit tests, each with real side-effect assertions (no data leak, zero rows created, `revoked_at`/credential unaffected), with authority delegated to decide any resulting technical question directly (auditing against project precedent, researching external prior art — UTAK, StoreHub — where internal evidence ran out).
- **2026-09-18** — **Writing those three tests surfaced a real, pre-existing defect** (commit `6c76799`): `terminalGet`/`terminalCreateEnrollmentToken`/`terminalRevoke` all used a bare `Terminal::where('store_id', ...)->findOrFail(...)`; the cross-store 404 status was correct, but the body was Laravel's own raw exception shape, not the frozen envelope — `ModelNotFoundException` is unconditionally rewrapped into `NotFoundHttpException` before any custom renderer for the original type can run, the same class of Laravel-internal rewrap already documented for `AuthorizationException` in A2, confirmed via a real HTTP round-trip. No existing `error-catalog.md` code fit "this terminal record does not exist for this actor" (every other 404 in the codebase is resource-specific and already catalogued, with no generic fallback), so `TERMINAL_NOT_FOUND` (404) was added as application code only — `TerminalNotFoundException` plus the three controller call sites — deliberately without touching `error-catalog.md` in the same commit, applying the lesson recorded for `ENROLLMENT_TOKEN_INVALID`.
- **2026-09-18** — **A3 second baseline reconstruction performed and validated** (a fourth reconstruction overall, same discipline as the prior three). See "A3 second baseline reconstruction" above for the full record: `main` preserved as `backup/pre-a3-terminal-not-found-reconstruction` (`6c76799`); `stage-4-baseline` through `stage-6b-baseline` rebuilt by cherry-picking every stage's actual substantive commits; `TERMINAL_NOT_FOUND` added to `error-catalog.md` at the new `stage-4-baseline` (`c231a31`); all 28 post-6B commits replayed cleanly with no manual conflict resolution. Content-equivalence: `git diff --stat` against the preserved backup shows exactly **one line** changed (the new catalog row), matching the deliberate not-forward-committed pattern. All 5 ancestry checks and all applicable isolation checks pass (`scripts/validate-baselines.sh`, 12/12, both before and after the tag move). Full regression green at the reconstructed tip before any tag moved: fresh migration and round-trip, `tests/Unit` 102/474, `tests/Database` 196/689, `tests/Feature` 1/1, A1/A2/A3 suites individually re-verified, all concurrency suites, OpenAPI YAML parse, Pint. `stage-4-baseline`/`stage-5-baseline`/`stage-6a-baseline`/`stage-6b-baseline` moved to their four-times-reconstructed hashes (`c231a31`/`b9f4d3f`/`ba97dfc`/`721cd72`); `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `main` force-updated to `ae6ffb2`; scratch branch deleted after the move. All five backup tags retained. **A3 is now genuinely governance-sealed; A4 remains authorized to begin on explicit instruction, not yet started.**
- **2026-09-18** — **A4 (authoritative User+Terminal+Store request-context composition) implemented**, under a standing autonomy directive authorizing independent technical decisions from sufficient project evidence. `App\Services\Auth\PosRequestContext` (a `final readonly` `{user, terminal, store}` DTO) and `App\Http\Middleware\ComposeAuthoritativeContext` (enforces `user.store_id == terminal.store_id` after `ResolveTerminalContext`, per §14 Ruling 1) unblocked `terminalCurrent`'s production route — moved out of the A3-closeout testing-only registration into `routes/web.php`'s real group. A store mismatch reuses `TERMINAL_NOT_ENROLLED` (403) rather than a new code: §16 item 6 explicitly forecloses inventing anything further here, and `openapi.yaml`'s existing `terminalCurrent` `403` description already reads correctly under that reuse — no Stage 4 amendment, no baseline reconstruction. A real, unrelated test-isolation bug was found and fixed while writing the coherence tests (a bare `login()` call mid-test ambiently inherited an earlier-captured session cookie and silently repointed it via `session()->regenerate()`) — documented in `module-a-auth-terminal-initialization.md` §20 as a general hazard of this test file's cookie-forwarding pattern. Full regression: `tests/Unit` 102/474, `tests/Database` 198/695, `tests/Feature` 1/1, A1/A2 suites individually re-verified, Stage 6A/6B/6C concurrency suites, Pint clean; `php artisan route:list` confirms `terminalCurrent` is now registered unconditionally. No baseline tag touched. **A5 (HTTP integration tests) and A6 (Stage 6C checkout integration) have not started.**
- **2026-09-18** — **A5 (HTTP integration test matrix) implemented**: audited all 18 items of `module-a-auth-terminal-initialization.md` §13 against A1/A2/A3/A4's existing test suites rather than assuming coverage. 16 items already had a passing test, cited by name in §21's table; two real gaps were closed with a new `tests/Database/ModuleAHttpIntegrationTest.php` (3 methods) — a sweep proving all 8 protected Module A routes reject an unauthenticated request (only 2 had ever been individually tested for this, and `auth/logout` had none), and a proof that `terminalCurrent` ignores spoofed `terminal_id`/`user_id`/`cashier_id` query input. The remaining two items (13a: shift/cashier mismatch; 17: checkout without terminal context, specifically against `saleFinalize`) both explicitly name a Stage 6C controller that doesn't exist yet — honestly recorded as deferred to A6, not force-closed by testing against the trusted parameter again. No `SaleController`/route was built, per instruction not to touch Stage 6C's controller in this pass. Full regression: `tests/Unit` 102/474, `tests/Database` 201/716, `tests/Feature` 1/1, Pint clean. No baseline tag touched; no Stage 4 amendment; no new application code. **A6 (Stage 6C checkout integration) has not started.**
- **2026-09-18** — **A6 (Stage 6C checkout integration) implemented — Module A (A0–A6) complete.** `SaleController`/`SaleFinalizeRequest`/`POST /sales` (registered under `routes/web.php`'s `web` group, per §14 Ruling 5) wire A4's `PosRequestContext` into `CheckoutService::finalize()` exactly as `stage-6c-sale-finalization.md`'s "Authentication boundary" section specified — `terminal_id`/`cashier_id` are never read from the request body, proven directly (`test_request_body_cannot_spoof_terminal_or_cashier_identity`). Added `SaleItemResource`/`PaymentResource`/`InvoiceSummaryResource`/`SaleDetailResource` matching `openapi.yaml`'s `SaleDetail` schema; `amount_tendered`/`change` are computed at serialization time from the immutable sale's own payments (`Sale` never persists them). No new HTTP-layer exception handling was needed: every exception `CheckoutService` can throw is already a `DomainException` subclass rendered through the existing generic renderer; `IdempotencyKeyRequiredException` is new application code but reuses the already-frozen `IDEMPOTENCY_KEY_REQUIRED` (400, ADR-010) catalog entry, which had no implementing class until now. The three resolver exceptions (fiscal-installation/inventory-location/tax-registration setup defects) remain deliberately uncaught, per their own docblocks — a setup-defect signal meant to surface as a genuine 500 and alert, not a business outcome to translate into a stable code; carried forward, not reopened. Re-verified §13 matrix items 13/13a/14/15/17 against the real controller, closing the two A5 had to defer. All nine of §16's Stage 6C unblock criteria are now met and demonstrated by passing tests. 7 new tests in `tests/Database/SaleFinalizationHttpTest.php`. Full regression: `tests/Unit` 102/474, `tests/Database` 208/736, `tests/Feature` 1/1, A1/A2/A3 suites and Stage 6A/6B/6C concurrency suites individually re-verified, `scripts/validate-baselines.sh` 12/12, Pint clean. No baseline tag touched; no Stage 4 amendment. **Stage 6C's freeze as `stage-6c-baseline` remains an explicit owner decision, not self-sealed here.**
- **2026-09-18** — **Owner instructed the Stage 6C freeze. `stage-6c-baseline` tagged at `de2a84c`.** See "Stage 6C freeze" above for the full record: `scripts/validate-baselines.sh` extended with a new `OWNERSHIP_stage_6c_baseline` pattern (the checkout domain/service layer plus A6's HTTP layer; Module A's own files deliberately excluded, being a separate cross-cutting initiative); `stage-6c-sale-finalization.md`'s disposition and this manifest's Stage tracker/At-a-glance/Frozen-corpus-rule sections updated to record the freeze; full regression green before tagging (`tests/Unit` 102/474, `tests/Database` 208/736, `tests/Feature` 1/1, Pint); `scripts/validate-baselines.sh` 14/14 after (7 ordering, 7 isolation — the new stage-6b→stage-6c ordering pair and all seven isolation checks pass, `stage-6c-baseline` itself `N/A` as the last tracked stage). A forward tag on `main`'s existing history — no reconstruction, no backup tag, Stages 1–6B entirely untouched. **Stage 6 (6A/6B/6C) is now fully frozen. Module A (A0–A6) and Stage 6 are both complete.**
- **2026-09-18** — **Stage 7 (Frontend) pass 1 implemented** (commit `754d530`): React + React Router, served same-origin from Laravel's existing `web` group (session-cookie + double-submit CSRF, already frozen in `api-design.md` — no CORS, no Inertia, no new auth mechanism). `GET /` now serves the SPA shell; a trailing catch-all route serves it for client-side routes too (excluding `api/...`, confirmed a genuinely unmatched API path still 404s as JSON). Scope kept deliberately narrow — `docs/06-ui/stage-7-frontend-initialization.md` records the reconnaissance: of every route `sitemap.md` proposes, only human auth/session (A1) and terminal enrollment (A3) have both a frozen contract and a real, tested backend today; POS checkout is blocked on Shift/FiscalDay endpoints that don't exist (`CheckoutService`'s OPEN-shift precondition has no HTTP path to satisfy it yet), and every back-office screen is blocked on its own not-yet-built controller. Built exactly the login screen and an authenticated landing page, nothing further. Verified end-to-end in a real browser against the real dev server and backend (not just code review): successful login, wrong-password rejection, `VALIDATION_FAILED` field errors, logout, session persistence across a full reload, and the catch-all's auth-redirect chain on a direct navigation to an unknown path. `app.blade.php` only loads `@vite` assets when a build or dev server actually exists, confirmed by removing `public/build` and re-running `tests/Database/AuthenticationSessionTest`'s CSRF-bootstrap test — still passes, so a fresh checkout's test suite has no hidden `npm run build` dependency. Full backend regression unaffected: `tests/Unit` 102/474, `tests/Database` 208/736, `tests/Feature` 1/1, Pint clean. **Next: Shift/FiscalDay backend endpoints, to unblock a real POS checkout screen.**
- **2026-09-18** — **`docs/06-ui/sitemap.md`'s two open questions resolved** (commit `347e60b`), grounded in targeted reference research against UTAK's and StoreHub's public help centers: `/back-office/dashboard` kept as a distinct route (both references treat dashboard and reports as separate features); POS-mode transaction lookup/void/refund reachability confirmed (both references default cashier-initiated corrections to the register itself, with StoreHub explicitly warning that back-office-initiated corrections can desync shift/sales reports — corroborating, not motivating, an already-locked TindaFlow invariant that every void/refund is an explicit new reversal transaction, never a mutation). Route tree unchanged; a forward edit to Stage-1-owned narrative content, not requiring reconstruction (no code depends on `sitemap.md`'s exact text, unlike `openapi.yaml`/migrations/domain classes).
- **2026-09-18** — **`shiftOpen`/`shiftCurrentGet` implemented, closing the gap `stage-7-frontend-initialization.md` disclosed** (commit `80c2061`): `ShiftOpenService` atomically resolves-or-opens the terminal's `FiscalDay` before opening the `Shift` (api-design.md §10), wrapped in `IdempotencyService` exactly like `CheckoutService`; `terminal_id`/`cashier_id` always resolved from `PosRequestContext`, never the body. Concurrency (invariants #32/#33/#34) proven under two real OS processes. A genuine frozen-contract gap was found and resolved directly (no owner escalation — a technical, not business/legal, decision): `shiftCurrentGet`'s 404 case has no code that fits without violating `error-catalog.md`'s code→status stability rule, so `NO_CURRENT_SHIFT` (404) is a new Stage 4 amendment, added as application code only in this commit. A timestamp-precision bug (`TIMESTAMPTZ(0)` truncation making `fiscal_day_was_opened` occasionally wrong) and a repeat of the already-documented bare-`login()`-mid-test hazard were both found via failing tests and fixed. 18 new tests. Full regression: `tests/Unit` 102/474, `tests/Database` 221/785, `tests/Feature` 1/1, Pint clean, OpenAPI parse clean. No baseline tag touched in this commit.
- **2026-09-18** — **Fifth baseline reconstruction performed and validated**, folding `NO_CURRENT_SHIFT` into the frozen Stage 4 catalog. See "Shift/FiscalDay backend... fifth baseline reconstruction" above for the full record: backup `backup/pre-shift-no-current-reconstruction` (`80c2061`); `stage-4-baseline` through `stage-6c-baseline` (now five tags, since Stage 6C's own freeze made it a tracked boundary) rebuilt by cherry-picking every stage's actual substantive commits — all 42 post-`stage-6b-baseline` commits replayed with no manual conflict resolution. Content-equivalence: exactly **one line** changed (the new catalog row), the deliberate not-forward-committed pattern. All 7 ancestry checks and all applicable isolation checks pass (`scripts/validate-baselines.sh`, 14/14, both before and after the tag move). Full regression green at the reconstructed tip before any tag moved. All five `stage-*-baseline` tags moved to their newly-reconstructed hashes (`0a2967a`/`a89a9ac`/`ec0676b`/`c1336c1`/`e75db55`); `stage-1-baseline`/`stage-2-baseline`/`stage-3-baseline` untouched; `main` force-updated to `a902499`; scratch branch deleted. All six backup tags retained.
- **2026-09-18** — **Stage 7 pass 2 (POS checkout screen), then Stage 8 (store setup and fiscal configuration) built**, recorded in their own sections above (commits `02c9d88`, `94f5c45`, `cbe53d4`). Stage 8 was the first time wholly new operations (`InvoiceSeries`, `InventoryLocation`, `fiscalInstallationAssignTerminal`) had no frozen draft; the owner chose to forward-commit five new error codes rather than reconstruct from `stage-1-baseline`. No tag moved.
- **2026-09-19** — **Stage 9 (shift close, cash movements, X-Readings, fiscal day close) and the sixth baseline reconstruction** (`SHIFT_NOT_FOUND`, `FISCAL_DAY_NOT_FOUND` folded into the Stage 4 catalog; `stage-4`..`stage-6c` tags moved to `9fdc2ca`/`71693bc`/`6591168`/`48fbbb6`/`822455a`). Applied the already-written rule rather than re-asking: a gap in an already-frozen response is reconstructed, a wholly new operation is forward-committed. See the section above.
- **2026-09-19** — **Stages 10 and 11: Reports backend and frontend.** All 15 reports with JSON and CSV export, zero frozen-corpus changes; the viewer and the responsive admin shell followed the Stitch mockups as visual direction only.
- **2026-09-19** — **Repository published to GitHub and the baseline tags pushed** (`main`; `stage-1-baseline` `d93a813`, `stage-2` `5a7382a`, `stage-3` `78a190c`, `stage-4` `9fdc2ca`, `stage-5` `71693bc`, `stage-6a` `6591168`, `stage-6b` `48fbbb6`, `stage-6c` `822455a`). From here a reconstruction requires a force-push, which changes how gaps in frozen responses are weighed (see Stage 13).
- **2026-09-19** — **Stage 12 (catalog management).** Zero frozen-corpus changes. Fixed a latent cross-store defect found on the way: checkout fetched products with no store scope, so a cashier could sell another store's product by id (`ca750f9`).
- **2026-09-19** — **Stage 13 (users management), with two owner decisions**: add `userActivate` (a wholly new operation, forward-committed) and forward-commit `USER_NOT_FOUND` as a one-time exception to the reconstruction rule because the repository is published. Lock-out protection is partial by the owner's choice (UI guard for self and the last admin; the API refuses only demoting the last active ADMIN).
- **2026-09-19** — **Stage 14 (stock receipts, adjustments and stock views)** (`ab2664f`), zero frozen-corpus changes. Fixed a latent defect that had broken invariant #44 since the first sale: checkout never updated `stock_balances`. `StockLedger` is now the sole writer of movements and balances and `inventory:rebuild-balances` repairs older data. Negative stock is allowed and warned about; writes go to the default location; untracked products have no stock. Full detail in `docs/06-backend/stage-14-stock.md`.
- **2026-09-19** — **Stage 15 (sales history, void and refund)**, zero frozen-corpus changes. Records two gaps in the frozen text and how they were resolved without touching it: a pending refund's lines are kept on the `REFUND_REQUESTED` audit event (invariant #72 forbids rows before completion), and the two reject operations, which need an `Idempotency-Key` but no terminal, are made retry-safe by state because idempotency is terminal-scoped. Full detail in `docs/06-backend/stage-15-sales-history-void-refund.md`.
- **2026-09-19** — **Stage 16 (audit log and electronic journal)**, zero frozen-corpus changes. The two list operations are built; `auditEventGet`/`journalEntryGet` are deliberately left unbuilt because their `404` has no registered code and registering one reopens the reconstruction question the Stage 13 exception left open — recorded as an owner decision in `docs/06-backend/stage-16-audit-journal.md` §3.
- **2026-09-19** — **Stage 17 (invoice view and reprint)**, zero frozen-corpus changes. Records how a reprint is journalled without a reprint event type (an `INVOICE` entry sourced from the reprint occurrence) and discloses that the checkout snapshot lacks the payments and fiscal-installation fields ADR-006's example lists, which a later snapshot version should add after the BIR field review. Full detail in `docs/06-backend/stage-17-invoice-reprint.md`.
- **2026-09-19** — **Stage 18 (store settings)**, zero frozen-corpus changes. A store without settings reads as a blank baseline and the first save must supply the three identity fields; checkout now snapshots the optional branch code, header and footer as additive keys of schema version 1 so the fields print on new invoices while every invoice already issued renders unchanged. Full detail in `docs/06-backend/stage-18-store-settings.md`.
